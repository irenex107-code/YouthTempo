# Pilot Manual Checklist

> **2026-09-19 会前版本说明：** 本功能分支的目标是当前试点完全不开放家长数据访问，历史 active 关系不能授权读取；成年人按独立路径运行。旧清单或审计中关于监护人可查看学生完整记录、解忧室只是预览、无普通倾听志愿者的描述仅保留为历史事实，不代表新目标或正式环境已更新。详见 `ROADMAP.md`、`docs/TEACHER_REVIEW_READINESS.md` 与部署手册。正式结论仍为 **READY WITH CONDITIONS**。

> **2026-09-20 MVP 隔离进度：** 本机第 55 份迁移升级、历史 active 家长旧 session 失权、带关系账号注销、花园/反馈和默认关闭入口已验证。下列人工、学校、真实设备和正式环境复选框仍开放；第一批准备顺序见 `docs/MVP_DEPLOYMENT_PREPARATION_CHECKLIST.md`，完成状态以 `ROADMAP.md` 为准。

每项记录日期、操作者和证据位置；使用虚拟账号，不在截图或报告中保存真实学生正文。

## 上线与身份

- [ ] 香港 Lighthouse 正式版本、Nginx、容器环境变量、健康检查与回滚版本已确认。
- [ ] Vercel GitHub 自动部署已断开；正式域名/ICP 状态已记录。
- [ ] QQ、163、Outlook 各完成一次 OTP 收件、8 位验证、刷新后 session、重发冷却和错误脱敏。
- [ ] 自有 SMTP 的 SPF、DKIM、DMARC 与发件人域已核对。

## 核心与设备

- [ ] iPhone Safari、安卓 Chrome、微信内置浏览器均完成登录→同意→SWEET→小结→历史→删除→退出。
- [ ] 320/360/375/390/414/768/1024/1280/1440 无横向溢出；Sleep→Wake 后题卡在视口内。
- [ ] 中英文 `/` 与 `/en/`、语言 cookie、`html lang`、关键错误和紧急提示已检查。
- [ ] 键盘操作、焦点、表单标签、图片 alt、错误提示和色彩可辨识完成抽查。

## 权限与安全

- [ ] 学生自主试用使用当前同意版本；14–17 岁本人确认后可生成并保存，14 岁以下仍被拒绝；数据库与导出记录的依据为 `student_self_pilot`，不得出现虚假的监护人 ID 或确认时间。
- [ ] 学生自主试用确认不会创建学校成员、老师分配或监护关系；无有效关系的家长、老师和学校无法读取记录，已存在关系仍严格按原 RLS 范围验证。
- [ ] 在独立 E2E Supabase 项目中，由两所虚拟学校的学生、家长、老师、校负责人、平台管理员分别验证允许和拒绝路径；不得向正式或恢复演练项目写入 fixture，结束后核对清理结果。
- [ ] 如首批试点包含校内心理老师或平台专业志愿者，使用虚拟账号分别验证学生支持老师、校内心理老师、平台审核人员和不同获准服务范围的个人专业志愿者；学校岗位、平台招募状态、个人资质、服务范围与学生授权必须是独立事实，不能依赖单一角色或 `school_id` 推断。
- [ ] 校内心理老师和平台专业志愿者只能访问本人已接受、授权有效且未到期的具体事项及最小化共享内容。跨校、跨人员、超出获准服务范围、未接受、已到期和已撤销路径均须拒绝，旧 session 立即失权。
- [ ] 结束学校岗位、平台招募或未来可选机构关系时，只撤销对应关系与未结束访问，不错误撤销仍有效的个人专业资质；个人资质过期或撤销会阻止新的专业承接并触发现有事项复核，但不把账号静默改成学生。
- [ ] 悄悄话信箱在当前试点只允许学生向已分配老师、试点值班负责人或自己留言，不出现家长收件；虚拟测试确认普通留言不会创建专业支持事项，也不会被平台专业志愿者读取。未来家长关系另行开放后再验证家长收件；既有确定性高风险升级路径保持有效。
- [ ] 当前试点不要求家长加入且不创建监护关系；后台成员、批量名单、关系创建和学生收件入口均不出现家长新增流程，18 岁及以上测试用户不返回家长关系。未满 18 岁的家长可选加入在另行批准前保持关闭。
- [ ] 分别使用 14–17 岁和 18 岁以上学生账号核对账户页与悄悄话信箱：前者显示自主试用、已分配老师及当前无家长收件，后者显示成年人独立使用且不出现家长流程；大学生公开页明确按个人基线与持续功能影响理解作息，不把晚睡、晚起、外卖或不固定作息单独解释为心理异常。
- [ ] 开放真实用户前只读清点 active 监护关系为 0；如发现历史关系先走可审计解除流程。正式开放成人路径前，另行批准 RLS 年龄/同意约束迁移，并用成人学生、未成年人学生、关联家长三个独立 session 验证直接 Data API 允许/拒绝边界。
- [ ] 撤销老师分配、监护关系、成员和学校后，旧 session 立即失权。
- [ ] 在隔离 E2E 项目验证仍关联 active `student_guardian` 同意的监护人注销：按已签署规则迁移学生同意状态，Auth 用户、session 与授权关系均被清理，且不会因 `student_consents_active_basis_check` 返回 500。
- [ ] 社区跨校/角色可见性、举报、`safety_review`、恢复、屏蔽、禁言和 SLA 完成。
- [ ] 当前试点界面不把家校医社区标为“解忧室”，也不显示 14–17 岁解忧室入口、灰色“暂未开放”卡片或候补名单；普通 API 和直接 URL 同样不能访问尚未实现或未批准的解忧室空间。
- [ ] 成年人解忧室开放前，在独立 E2E 项目验证：受邀成年学生确认规则后自动加入唯一的跨校“大家的解忧室”；学校验证、登录刷新或名单更正均不会创建本校 room、membership 或 topic。学校或 cohort 管理人员不能读取聊天正文、对外身份或成员名单。
- [ ] 验证学生主动退出公共房间后，规则状态、登录或学校验证同步不会静默把本人加回；只有本人明确重新加入才创建新的成员期。
- [ ] 解忧室消息不设自动到期，但历史严格按每一段有效 room membership 过滤：新成员看不到加入前消息，退出或被移出后重新加入看不到离开期间消息；伪造游标、旧链接和直接 message ID 同样被拒绝。断网、关闭页面或退出登录不错误结束 active 成员期。
- [ ] 大家房间的学校标签默认隐藏，只能由学生本人主动展示当前验证学校；不能自由填写或显示院系、年级、班级。关闭后，平台内当前和既往消息均不再返回标签，并提示无法撤回他人已经看到或截取的信息。
- [ ] 在公共房间验证匿名、房间昵称、经验证姓名三种对外身份：默认房间昵称，不允许逐条消息切换，切换后当前和既往消息动态使用最新模式；匿名仍保留平台安全映射。姓名确认工作流未完成时实名全局关闭；完成后仅 active 学校名单确认可启用，不得回退到 `profiles.display_name`、邮箱、Auth metadata、自由输入或现有邀请姓名。
- [ ] 使用两所虚拟学校验证经验证姓名来源：本校有权名单管理者可确认、更正和撤销姓名，跨校、普通配置管理员、值班和审核人员均被拒绝；确认接口不返回学生是否选择实名、房间 membership 或活动。测试数据不得包含真实身份证件，首批流程不得要求上传证件。
- [ ] 验证姓名确认进入更正、撤销、过期、学校验证失效或姓名内容变化时，公共房间立即回退到房间昵称，当前及历史消息同步变化且不向同伴广播原因；重新确认后不会自动恢复实名，学生须重新选择。
- [ ] 学生端、普通 API、数据库 fixture 和 Realtime 均不存在本校聊天室入口、灰色占位、候补、隐藏预建或可订阅 topic；相关未来升级只记录在 `PEER_SPACE_FUTURE_UPGRADES.md`。
- [ ] 公共聊天室只在已有 1 名当班、1 名独立备班/安全联系人、交接和安全升级路径时显示“值班开放”；验证 30 秒续租、60 秒提醒、120 秒无有效租约自动只读。客户端不能直接 Broadcast，危险消息在广播前进入安全路径，断线后以鉴权 API 补齐且不恢复已删除、屏蔽或撤权内容。
- [ ] 在数据库级并发测试中验证同一房间重叠班次的约束与租约回收一致：一条班次过期时，不能错误关闭另一条仍由有效班次覆盖的房间；若产品决定每房间只允许一条有效班次，则并发创建和交接必须被数据库约束正确拒绝或原子切换。2026-09-19 临时隔离数据库已覆盖并发签到拒绝及顺序交接/过期场景；交接与回收同时竞争、完整 RLS 矩阵和定时降级仍待验证，故保持未勾选。
- [ ] 学生端仅向服务端确认的受邀成年学生显示解忧室卡片；未登录、未受邀和 14–17 岁不显示空位或占位，直接 URL 使用不枚举原因的通用不可用状态。顶部身份入口、中学生页、家长页、家校医社区和悄悄话信箱保持独立。
- [ ] 在 320px、桌面、真实 iPhone、Android 和微信内置浏览器验证聊天室软键盘、粘性输入区、发送中/失败、断线补齐、有新消息按钮、只读/暂停、学校展示确认、举报、屏蔽、删除和退出；新消息无障碍提示不得自动朗读整段敏感正文。
- [ ] 解忧室工作人员按 `room_duty`、`content_moderator`、`safety_duty`、`config_admin` 分别使用独立账号或明确切换当前 capability；验证无相应能力的普通平台管理员、学校、老师和专业支持者不能进入工作台或读取 API，同一人兼任也不能获得未使用能力的默认全量访问。
- [ ] 用虚拟内容完成一次值班签到、开放前核对、开房、官方提示、举报领取、保留/移除、限时禁言、紧急暂停、安全事项、受限身份查看、下一班接受和无人接班自动只读演练；所有处置均有结构化理由、不可变审计和失败恢复，不使用浏览器 prompt。
- [ ] 用虚拟账号验证 1 小时/24 小时禁言可单人执行，7 天禁言或移出必须第二人批准且提供 7 天申诉；身份映射常规查看必须双人批准，迫切安全风险的单人例外在 24 小时内补审。学校只可看到房间配置、值班准备等纯运营状态，不能获得学生人数、活跃度、消息、主题、举报或其他使用统计。
- [ ] 中英文高风险输入在 Check-in、Mood Journal、已关闭的 Talk API、Worry Time、Referral 均不调用普通 AI；Talk 早先消息命中后不会被后续普通消息冲掉；普通压力、难过、疲惫不过度危机化。
- [ ] 五个工具的固定紧急文案、词表误报/漏报样例和“立即联系现实支持”的停止路径已经心理专业、安全/隐私及学校负责人共同审核并留存版本与日期。
- [ ] 试点值班只收录学生主动联系和无学校承接的 `safety_review` 留言；普通私人记录不会进入队列。未登录、普通用户及学校角色不能读取平台值班 API。
- [ ] 使用虚拟留言验证值班邮件只含事件编号、时间和后台链接，不含姓名、邮箱或正文；发送失败时留言仍在队列且学生端不会声称邮件已送达。
- [ ] 两个网页 AI 摘要工具及小程序 Check-in 均显示当前告知版本 `ai-notice-2026-08-18-v2`；未登录、有效同意缺失、未确认或版本过期的普通请求不会调用 provider；所有普通生成结果持续显示“AI 辅助记录小结·非评估或诊断”。
- [ ] 首批试点保持受限 AI 小结开启；现场核对 `AI_ALLOWED_PROVIDER_HOSTS`、`OPENAI_BASE_URL`、`AI_ALLOWED_MODELS` 与 `OPENAI_MODEL` 相互匹配。需要暂停时设置 `AI_GENERATION_ENABLED=false`，确认普通生成返回安全的 503 且危机固定路径仍可用。
  - 2026-09-07：经试点发起人明确授权，正式环境 `api-router.fly.dev` / `claude-haiku-4-5` 与两项 allowlist 已核对匹配；候选容器与切换后正式容器的无用户数据合成请求均返回 HTTP 200，公网 `/api/health` 正常。尚未在本次操作中切换总开关验证 503/危机路径，因此本项不关闭。
- [ ] Worry Time 显示固定规则版本和“非 AI 生成”边界；普通整理不需要 AI 告知且不会调用 provider。
- [ ] Talk 页面显示首轮学校试点关闭状态、无输入框且不发送内容；普通 API 请求返回 `talk-closed-2026-08-18` 关闭版本，旧客户端的明确高风险输入仍优先进入固定现实支持路径。确认本项不影响家长端 SWEET Talk。
- [ ] Referral 显示 `referral-rules-2026-08-18` 规则化、非 AI 路径；四类规则分支、自由补充文字不参与评分/不发送 provider、危机优先和真人升级条件已经心理专业及学校负责人审核。
- [ ] 提示注入、超长输入、重复提交和上游失败只返回用户安全信息。

## 数据与运营

- [ ] 正式学校试点前关闭学生自主入组，发布新的学校同意版本，并重新完成适用的学生/监护人告知与确认；不得把自主试用依据自动视为学校试点确认。
- [ ] 如本批次开放监护人访问，监护人访问政策已签署并与学生/家长告知一致；纯学生自主试用记录为不适用且确认没有创建监护关系。
- [ ] 账号导出、删除、同意撤回、保存期限和学校退出完成一次闭环；明确区分账号存续内容、解忧室无自动到期消息、结案后最多 180 天的必要证据，以及最长 24 个月的去标识化操作审计。
- [ ] 加密异地备份与隔离恢复通过，记录 RPO/RTO、校验和及删除重放结果。
- [ ] 登录、保存、AI、社区四类受控失败触发日志和外部告警，且无个人数据或凭证。
- [ ] 学生自主试用值班负责人、值班邮箱、值班时段、预计首次查看时间、替补联系人、非值班时段提示和线下紧急路径已登记。
- [ ] 校内心理老师或平台专业志愿者参与前，个人资质标准、获准服务范围、主要承接人、备援与交接、非值班路径、访问结束和数据删除责任已登记；YouthTempo 不保存完整咨询、诊断或治疗病历。机构合作如未来启用，再单独登记合作期限和双方责任。
- [ ] 学校现场联系人、危机升级、举报值班、故障公告和暂停试点决策人已登记。

最终批准：产品 ______；技术 ______；安全/隐私 ______；学校 ______；日期 ______。

## 2026-09-19 新方向专项验收（均未因本地构建自动完成）

- [ ] 在独立 E2E 项目执行本次迁移；对历史 active 监护关系验证 guardian 对 SWEET、Check-in、消息、支持事项及直接 Data API 均无权，旧 session 立即失权，注销不返回 500。正式历史关系只读清点及修复另待批准。
- [ ] 用 14–17 与 18–25 两个独立账号核对工作台、花园和完整 SWEET 自主节奏；伪造年龄参数不能跨入成年空间。
- [ ] 用受邀/未受邀成年人、未成年人、退出房间成员和值班/备班角色验证跨校主题房间的消息读取、发送、短轮询、审核、举报、屏蔽、静音和失权。
- [ ] 用四类申请人员与平台审核人员验证申请、私有材料、批准/暂停、具体事项授权、主备交接、转介、结束后撤权；学校值班老师不得自由注册。
- [ ] 验证咨询公开开关默认关闭，内部许可账号才能创建正式咨询事项；安全反馈仅供授权人员人工处理，无自动淘汰或公开排行。
- [ ] 验证轻量反馈七天冷却、跳过不阻断、危机不提示、后台仅汇总；中英文桌面与移动真机核对无障碍与回访。
