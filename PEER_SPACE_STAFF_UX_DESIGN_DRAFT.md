# 解忧室值班、审核与安全工作台交互设计草案

> **2026-09-19 新方向提示：** 本文保留早期决策/设计脉络；与 `ROADMAP.md` 会前版本方向冲突的唯一公共房间、可展示经验证姓名、无普通倾听志愿者或尚未实现的描述均以新路线图和 `docs/TEACHER_REVIEW_READINESS.md` 为准。代码仍处开发分支，正式环境未开放。

状态：**2026-09-17 工作人员端 UX 草案及本地授权基础说明。非生产页面预览已存在；工作人员 capability + scope、撤销审计、班次、签到与 120 秒租约已在尚未应用的 migration 和默认关闭的服务端 API 中实现。尚未连接数据库、配置真实人员/排班、接入持续租约回收、60 秒提醒、消息、Realtime、审核/安全队列或任何真实学生内容，生产工作台继续关闭。**

关联基线：

- `PEER_SPACE_PRODUCT_DECISION.md`
- `PEER_SPACE_ADULT_MVP_DESIGN_DRAFT.md`
- `PEER_SPACE_STUDENT_UX_DESIGN_DRAFT.md`
- `PEER_SPACE_TECHNICAL_DESIGN_DRAFT.md`
- `SECURITY_PERMISSION_MATRIX.md`

## 1. 设计目标

让已经获得明确能力并处于有效班次的工作人员能够完成：

1. 查看自己今天负责的房间、班次、备班和安全升级路径。
2. 完成签到和开放前核对后，开启、转只读或暂停获派房间。
3. 以清晰的官方身份维护实时聊天秩序，不伪装成学生或咨询师。
4. 只在职责范围内查看聊天、举报或高风险事项，并留下可审计处理记录。
5. 对具体消息所对应的内部成员采取限时禁言或移出措施，而不是依赖对外身份或浏览完整学生名单。
6. 在现实安全处置确有必要时，通过受限步骤查看账号映射。
7. 把未结事项、房间状态和责任完整交给下一班，不能依赖口头或私人聊天软件交接。

工作台不能把“平台管理员”“专业资质”“学校成员”自动解释为全部内容访问权。即使首批由同一名专职人员兼任多项职责，也必须以当前能力上下文操作并分别记录审计。

## 2. 与现有后台的关系

### 可以沿用

- 现有后台的卡片、状态标签、摘要数字、处理说明、状态历史和刷新模式。
- `PilotDutyMessageQueue` 中“正文按需展开、处理说明必填、历史不可覆盖”的思路。
- `CommunityModerationQueue` 中举报分类、优先级、移除/恢复、限时禁言和处理记录模式。
- 现有 `button-primary`、`button-secondary`、`card`、`cream`、`mist`、`sage` 视觉语言。

### 不能直接沿用

- 不能继续把实时值班、社区审核、专业认证和学校管理纵向堆在一个超长 `/admin` 页面。
- 不能只靠 `isPlatformAdmin` 控制整个工作台。
- 不能在队列列表默认返回每条聊天正文、作者账号、邮箱或隐藏学校。
- 不能使用 `window.prompt` 完成高风险移除、禁言、恢复或身份查看；必须使用结构化 dialog、理由字段和确认摘要。
- 不能把当前非实时留言值班队列当作聊天室排班和房间控制系统。

## 3. 能力上下文

| 能力 | 主要任务 | 默认可见数据 | 默认不可见数据 |
|---|---|---|---|
| `room_duty` | 签到、开房、实时秩序、官方提示、交接 | 获派房间当班期间的公开聊天和房间状态 | 真实身份、隐藏学校、非获派房间、完整成员名单 |
| `content_moderator` | 处理举报、复核消息、禁言/移出 | 获派举报目标及必要上下文 | 普通全量房间、举报人身份、账号映射、隐藏学校 |
| `safety_duty` | 高风险事件、现实升级、必要身份查看 | 获派安全事件及最小上下文 | 普通举报、无关聊天、未获理由的身份映射 |
| `config_admin` | 房间、邀请、规则、人员能力、排班配置 | 配置、自动建房状态和可信姓名来源状态 | 聊天正文、账号映射、举报正文、安全身份详情 |

同一账号具有多项能力时，顶栏显示“当前以：房间值班 / 内容审核 / 安全值班 / 配置管理身份操作”。切换能力后重新加载授权范围；任何写操作记录实际使用的 capability，而不是笼统记录“管理员”。

## 4. 信息架构与路由

| 路径 | 页面 | 所需能力 |
|---|---|---|
| `/admin/peer-space` | 我的工作台：今天班次、待办和获派房间 | 任一 active 解忧室能力 |
| `/admin/peer-space/rooms/[id]` | 实时房间值班视图 | 当前有效 `room_duty` 班次或明确获派的审核能力 |
| `/admin/peer-space/moderation` | 举报与内容复核队列 | `content_moderator` |
| `/admin/peer-space/safety` | 高风险安全事项队列 | `safety_duty` |
| `/admin/peer-space/shifts` | 排班、签到、交接和历史 | `room_duty`；配置视图需 `config_admin` |
| `/admin/peer-space/config` | 房间自动配置、试点邀请、规则、人员能力和可信姓名来源 | `config_admin` |
| `/admin/peer-space/audit` | 本人职责范围内的操作审计 | 按 capability 和 scope 授权 |

现有 `/admin` 只在当前账号确有解忧室能力时显示一张“解忧室工作台”入口卡，不显示消息摘要或学生信息。无能力用户直接访问上述路由时返回通用无权限页面。

## 5. 工作台外壳

### 顶栏

- 页面标题“解忧室工作台”。
- 当前能力上下文和获派范围。
- 班次状态：未签到、当班中、等待交接、已结束。
- 系统状态：Realtime、消息 API、审核队列和提醒通道的健康摘要。
- 紧急“将获派房间转为只读”操作，仅在当前能力允许时显示。

### 左侧导航或移动端菜单

只显示当前能力可访问的项目。隐藏菜单不是唯一权限控制，服务端仍逐路由和逐操作鉴权。

### 首页摘要

- 今天班次和下一次签到时间。
- 当前获派房间及状态。
- 待处理普通举报数量。
- 待处理安全事项数量。
- 需要交接的事项数量。
- 配置或提醒异常。

摘要数字只用于工作排序，不显示敏感正文、学生姓名、学校组合或平台全局排行榜。

## 6. 开始班次

### 6.1 班次卡

显示：

- 负责房间、计划起止时间和当前时区。
- 当班人员本人、备班人员和升级联系人是否已确认。
- 房间计划状态。
- 上一班是否已交接。
- Realtime、历史 API、审核队列和支持资源是否可用。

每个开放房间必须同时具备 1 名正在值守人员和 1 名独立备班/安全联系人；首批默认每名当班人员只负责 1 个开放房间。

### 6.2 签到前核对

工作人员必须逐项确认：

1. 本人可以持续使用当前安全设备和网络。
2. 备班人员当前可联系。
3. 现实安全升级联系人和路径有效。
4. 房间规则、支持资源和暂停负责人没有过期。
5. 已阅读上一班未结事项摘要。

“签到”只激活本班次和相应最小访问，不自动开启房间。

### 6.3 开启房间

- 只有在计划时段、本人已签到、备班和安全路径有效、关键服务健康时才显示“开放实时聊天”。
- 开启 dialog 显示房间、受众范围、计划结束时间和当前当班人员；必须再次确认。
- 首期唯一房间明确提示“跨学校成年学生”；不显示成员名单，也不提供本校房间开关。
- 开启成功后学生端显示“值班开放”。

若条件不完整，界面指出缺少哪项内部准备，但不允许用前端强行跳过。

## 7. 实时房间值班视图

### 7.1 页面布局

桌面端采用三部分：

- 顶部房间状态栏：房间名、跨学校公共范围、当班剩余时间、连接状态、只读/暂停操作。
- 主区实时消息：与学生端顺序一致，但增加内部操作入口。
- 右侧窄栏：当班清单、未结房间事项、官方提示模板和支持资源；不放完整成员名单。

移动端优先保证查看最新消息、官方提示、举报升级和转只读；复杂配置和批量排班提示使用桌面完成。

### 7.2 工作人员看到的作者信息

- 只显示学生在当前房间选择的对外身份：稳定匿名标识、房间昵称或经验证姓名；工作人员看到的内容与同房间学生一致。
- 即使学生选择经验证姓名，`room_duty` 和 `content_moderator` 也不能点击姓名展开账号、邮箱或学校关系。
- 若学生已在大家房间主动展示学校，工作人员可看到与其他学生相同的公开学校标签；不能点击标签查看学校关系。
- 学生隐藏学校时，`room_duty` 和 `content_moderator` 不显示学校，也不能从 cohort 推断。
- 学生在公共房间主动展示学校时，标签仅作为同伴可见信息；它不代表存在本校聊天室或工作人员获得学校关系访问。
- 不显示真实姓名、邮箱、用户 ID、精确年龄、院系、班级或登录状态。

### 7.3 实时行为

- 新消息在底部时自动追加；工作人员查看旧内容时显示“有新消息”，不强制跳转。
- 消息安全检查中不出现在普通实时流。
- Realtime 事件丢失或重连后，通过鉴权 API 按游标补齐。
- 每条消息的更多菜单提供“提交审核”“建议禁言”“升级安全事项”，而不是直接暴露账号详情。
- 房间值班打开聊天视图和班次窗口会留下访问审计；不需要为每次滚动重复写入正文审计。

### 7.4 不提供成员列表

工作人员不能按房间浏览所有学生、按学校筛选、搜索昵称或导出成员。针对学生的操作从具体消息、举报或安全事件开始，降低无目的浏览和身份拼接风险。

## 8. 官方房间消息

### 官方身份

- 对学生统一显示“解忧室值班”，可附“官方”标签。
- 不显示工作人员私人姓名、电话或邮箱。
- 内部审计记录实际操作者。

### 允许用途

- 欢迎与房间边界。
- 尊重交流期待和避免透露个人信息的提醒。
- 技术故障、即将只读、暂停或关闭说明。
- 引导查看独立支持资源。

### 发送流程

1. 选择审核过的模板，或在权限允许时输入不超过 300 字的自定义提示。
2. 预览学生端看到的官方身份、房间范围和正文。
3. 确认后通过独立 official endpoint 发布并审计。

官方消息不能诊断、替学生下结论、公开点名某个昵称的安全状态、承诺咨询师立即回应或要求学生转到工作人员私人联系方式。

## 9. 房间控制

### 转为只读

- `room_duty` 可在获派班次内立即转为只读，适用于交接缺口、服务降级或当前无法持续值守。
- 操作 dialog 显示学生端公开提示、下一次计划开放时间和内部原因。
- 内部原因进入审计，学生只看到用户安全摘要。
- 服务端立即拒绝发送，不只禁用输入框。

### 紧急暂停

- 当出现持续攻击、疑似大范围隐私暴露、安全升级路径不可用或系统无法可靠审核时，`room_duty` / `safety_duty` 可以立即暂停获派房间。
- 暂停不等待常规审批；操作者必须在事后规定时间内补充原因和通知配置负责人。
- 学生端停止普通历史和发送，保留本人事项、退出和现实支持入口。

### 重新开放

- 常规只读可由下一位已签到值班人员在重新核对后开放。
- 因安全事件暂停的房间不能由原操作者单独随手恢复；需要获批的安全或配置复核流程。

## 10. 举报与内容审核

### 10.1 队列列表

默认只显示：事件编号、房间类型、举报类别、优先级、提交时间、目标首次复核时间、当前负责人和状态。不在列表预览完整正文、作者账号、举报人或隐藏学校。

筛选仅包括本人获派范围、状态、优先级和时间，不提供按学生、学校或昵称搜索。

### 10.2 领取事项

- 点击“开始处理”后原子分配给当前审核员，防止两人同时作出冲突决定。
- 已被领取的事项显示负责人和更新时间；其他审核员可查看最小状态，但不能同时提交处置。
- 超时或离班时可由明确交接流程转派，不能偷偷改负责人。

### 10.3 详情页

按层级展开：

1. 举报类别和举报人填写的限长说明；不显示举报人身份。
2. 目标消息和前后少量必要上下文。
3. 既往与该内部成员在本房间相关、且对当前决定必要的已结处置摘要；不提供完整发言历史。
4. 允许操作：保留、移除、限时禁言、解除限制、移出解忧室、升级安全事项。

每项操作使用结构化 dialog：选择依据规则、填写简短说明、预览影响范围后确认。禁止用浏览器 prompt。

### 10.4 处置范围

- 移除消息不等于禁言账号。
- 禁言可按当前房间或整个 `18_plus` 解忧室设置明确期限。
- 1 小时或 24 小时禁言可由一名获准内容审核员决定；7 天禁言或移出解忧室必须由第二名审核员批准，等待期间最多先执行 24 小时临时限制。
- 学生可在处置后 7 天内提交一次申诉；申诉不得由原决定人单独结案。
- 解除限制同样需要原因并产生新审计事件，不能删除原记录。
- 举报人可看到本人事项状态，但不看到处理人、具体惩罚或其他学生信息。

## 11. 高风险安全事项

### 11.1 安全队列

与普通举报完全分开，按紧急程度和目标响应时间排序。提醒渠道只发送随机事件编号、时间、优先级和工作台链接，不包含正文、昵称、学校或联系方式。

### 11.2 事项详情的渐进披露

第一层默认显示：

- 事件编号、来源、房间、时间、当前负责人。
- 触发类型，例如“确定性安全规则命中”或“人工升级”，不显示模型诊断或风险分数。
- 目标消息和最少必要上下文。
- 当前是否有现实升级路径和有效联系人，但不默认展开身份。

第二层只在工作人员确认需要现实安全处置时出现“请求查看账号信息”。

### 11.3 查看身份

1. 选择或填写具体理由，关联 active safety case。
2. 界面列出将解锁的字段和最小必要原则。
3. 原则上进入双人复核，由第二名具有对应 scope 的 `safety_duty` 人员批准；存在迫切现实安全风险时允许一人先行查看，但须单独记录紧急例外并在 24 小时内补做第二人复核。
4. 获准后在限时侧栏中显示必要账号和联系/学校关系，不允许复制整页或批量导出。
5. 查看人、复核人、理由、时间和字段范围写入不可变审计。

身份侧栏关闭或授权过期后立即清除；普通内容审核、房间值班或配置能力不能复用该访问。

### 11.4 安全处置记录

状态建议：`new` → `acknowledged` → `action_in_progress` → `monitoring` / `handed_off` → `resolved`。

- 每次变化记录负责人、时间、理由和下一步。
- 现实联系只记录渠道、时间和结果，例如“已接通”“未接通”“已交接”，不复制聊天正文或写未经证实的诊断。
- “已查看”“已发送提醒”不能直接等于“已解决”。
- 结案必须记录为什么可以结束、是否仍有后续负责人及身份访问是否关闭。
- 不把普通情绪、孤独、疲惫或求倾听自动升级给学校、家长或专业人员。

## 12. 限制成员

从具体消息或已获派事项发起，工作人员只操作当前对外身份背后的内部目标，不先看到真实账号。

### 限时禁言

- 选择范围：当前房间或整个成年人解忧室。
- 选择期限：1 小时、24 小时或 7 天；7 天自动进入第二人复核，等待期间先执行不超过 24 小时的临时限制。
- 选择规则依据并填写说明。
- 预览学生仍可执行的操作：查看本人状态、删除、举报、屏蔽、退出和查看支持资源。

### 移出

- 只结束解忧室 membership，不删除 YouthTempo 账号或其他本人记录。
- 必须由第二人批准，并提供明确依据和 7 天申诉说明；等待期间最多先执行 24 小时临时限制。
- 不自动通知学校，也不公开告诉房间其他学生。

### 解除限制

- 显示原操作、原因、期限和历史复核。
- 解除会追加审计事件，不覆盖原记录。

## 13. 交接与结束班次

### 13.1 交接摘要

系统自动汇总：

- 当前房间状态和连接健康。
- 未结普通举报数量与负责人。
- 未结安全事项数量、优先级和负责人。
- 当前禁言/移出复核数量。
- 下一班、备班和升级路径状态。

交接摘要引用事件编号，不复制聊天正文、账号、邮箱或学校标签组合。

### 13.2 交接流程

1. 当前值班人确认每个未结事项的下一步。
2. 下一班人员签到并主动接受交接。
3. 系统记录双方、时间和移交范围。
4. 下一班接受后，前一班的实时房间正文权限到期并取消订阅。

本地值班基础暂允许同一房间一条当班 `active` 与一条交接中 `handoff_pending` 短暂并存；不允许两个当班或两个交接中班次。此为待评审的技术约束，不代表真实交接 UI、接班接受动作或内容权限已接入。

### 13.3 无人接班

房间不能在没有有效当班人的情况下继续显示 `staffed_open`。计划结束前持续提示；到期无人接班时自动转只读，并向备班/配置负责人发送不含正文或身份的提醒。

### 13.4 结束班次

只有房间已经交给下一班、转为只读或暂停，且所有紧急事项已有明确负责人时才能正常结束。强制结束属于异常操作，必须写明原因并触发运营提醒。

## 14. 配置管理

`config_admin` 使用独立配置视图，不显示聊天正文。

### 可管理

- `18_plus` 空间状态和总开关。
- 公共房间的计划开放时间和暂停负责人；首期不创建、配置或预留本校房间。
- cohort 邀请、成员状态和规则版本。
- `room_duty`、`content_moderator`、`safety_duty` 能力及 scope、起止时间和撤销。
- 排班模板、备班、交接规则和异常提醒。
- 学校标签公开名称来源和是否仍有效；不能替学生开启展示。
- 经验证姓名功能的全局就绪状态和学校名单确认覆盖状态；配置管理员不能直接填写、覆盖或导出学生姓名。当前可编辑 `profiles.display_name`、邮箱、Auth metadata、自由输入或缺少独立确认记录的邀请姓名不得被配置成可信姓名。
- 向学校只提供房间是否已配置、值班准备是否完成等纯运营状态；不提供学生人数、活跃度、消息、主题、举报或其他使用统计。
- 支持资源版本、现实安全升级路径和系统 kill switch。

### 不可管理

- 不能用配置页面搜索或浏览学生聊天。
- 不能查看账号—房间身份映射。
- 不能代学生显示学校。
- 不能代学生选择匿名、昵称或经验证姓名，也不能借学校关系创建本校房间。
- 不能查看哪些学生选择了经验证姓名，也不能从姓名确认状态跳转到其房间活动。
- 不能因为学校提出要求就导出成员、昵称、正文或举报详情。
- 不能显示本校聊天室申请、候补、人数门槛或自动建房控制；未来升级须重新批准。
- 不能把专业核验、学校成员或平台管理员自动转成解忧室工作权限。

## 15. 系统异常与安全降级

| 异常 | 工作台表现 | 房间处理 |
|---|---|---|
| Realtime 断开 | 顶栏醒目提示并尝试重连；保留本地未提交处理说明 | 消息 API 仍安全时可短时读取；如同时导致 120 秒没有有效当班租约则转只读 |
| 历史/发送 API 异常 | 停止显示“已处理/已发送”成功状态 | 转只读，不让学生误以为消息已送达 |
| 安全检查不可用 | 红色内部告警，不允许绕过 | 立即暂停普通发送 |
| 审核队列不可用 | 禁止新处置并保留未提交草稿 | 根据批准时长转只读或暂停 |
| 值班心跳丢失 | 每 30 秒续租；60 秒未续租时倒计时提醒并通知备班 | 120 秒没有任何有效当班租约时自动只读 |
| 提醒邮件/短信失败 | 事件仍保留在安全队列，显示提醒失败 | 不丢事件，不把邮件失败等同于无人负责 |
| 学校/成员关系撤销 | 清空相应缓存和订阅 | API立即失权；旧频道最长 5 分钟完全断开 |

任何降级都不能公开数据库错误、token、内部规则、学生身份或工作人员私人联系方式。

## 16. 通知

- 工作人员通知只包含事件类型、随机编号、优先级、时间和工作台链接。
- 不发送消息正文、学生对外身份、学校、邮箱、电话、token 或安全规则命中词。
- 普通举报提醒和高风险安全提醒使用不同渠道和优先级。
- 通知送达不等于事项已被查看；必须由工作人员在工作台主动确认。
- 非当班人员默认不接收普通房间提醒；备班只在交接失败、值班失联或明确升级时接收。

## 17. 审计与隐私

必须记录：

- 签到、开房、只读、暂停、恢复和结束班次。
- 进入获派实时房间的工作会话。
- 领取、转派、保留、移除、禁言、移出、恢复和结案。
- 官方消息发布。
- 安全事项状态和现实交接。
- 账号—房间身份映射查看、字段范围和复核。
- 能力授予、撤销、到期和 scope 变化。

审计列表默认不复制正文。需要查看被处理内容时，仍按当时或当前批准规则进入受限详情。必要举报/安全证据在结案后最多保留 180 天；不含正文、明文邮箱或用户编号的操作审计最多保留 24 个月。审计不可由普通管理员删除或改写。

工作人员设备要求：自动锁屏、离开自动失效、禁止共享账号、限制下载和打印；浏览器页面标题、最近访问和系统通知不包含学生内容。

## 18. 无障碍、双语与移动端

- 新增工作人员文案同时提供 `zh-CN` 和 `en`，内部稳定状态值不翻译。
- 状态和优先级不能只靠红黄绿颜色；同时显示文字与图标语义。
- 实时新消息使用简短 `aria-live` 状态，不自动朗读完整敏感正文。
- Dialog 支持焦点锁定、Escape、关闭后焦点恢复和明确危险操作确认。
- 所有处理说明可通过键盘完成；不使用浏览器 prompt。
- 移动端提供当班、紧急暂停、安全确认和交接的必要路径，但批量排班、人员授权和复杂配置建议在桌面完成。
- 320px–1440px、系统缩放和 reduced motion 都需要验证。

## 19. 工作台实现验收标准

- 无解忧室 capability 的平台管理员、学校负责人、老师和专业支持者不能看到工作入口或读取 API。
- 同一账号切换能力后，页面、API scope 和审计 capability 一致。
- 签到不自动开房；缺少备班、安全路径或关键服务时不能开放。
- 无人接班或连续 120 秒没有有效当班租约时，房间由服务端自动转只读。
- 学生隐藏学校时普通值班和审核看不到学校；学生主动展示时只看到同伴可见的学校名称，不能展开关系。
- 工作人员只看到学生当前选择的匿名标识、房间昵称或经验证姓名；无可信姓名来源时不能显示或回退到账号名称、邮箱。
- 姓名确认失效或变化后，工作人员视图与学生端同时回退到房间昵称，不显示旧姓名或失效原因；重新确认也不会自动恢复实名。
- 工作人员没有成员目录，所有限制操作从具体消息、举报或安全事件发起。
- 举报领取具有并发保护，处置理由必填，移除、禁言、移出和恢复分别记录。
- 高风险事项不与普通举报混合；身份查看有 active case、明确理由、限时展示、双人批准和不可变审计，紧急单人例外在 24 小时内补审。
- 1 小时/24 小时禁言、7 天禁言/移出双人复核和 7 天申诉路径均完成允许与拒绝测试。
- 通知失败不丢事项，通知内容不含正文或身份。
- 前后台在断线、API失败、安全检查不可用、只读、暂停和旧权限撤销时保持一致。
- 真实排班人员完成一次开房、官方提示、普通举报、紧急暂停、安全事项、身份受限查看、交接和无人接班演练后，才可申请开放真实学生。

## 20. 本草案不包含

- 具体工作人员招聘标准、劳动安排或报酬。
- 心理咨询师的专业服务记录和咨询管理系统。
- 数据库、RLS、Realtime、API或页面实现。
- 生产账号、真实排班、真实提醒或学生数据测试。
- 14–17 岁解忧室的学校/监护人工作流。

工作人员端 UX 批准：产品负责人 ______ / ______；技术负责人 ______ / ______；隐私或安全负责人 ______ / ______；审核运营负责人 ______ / ______。
