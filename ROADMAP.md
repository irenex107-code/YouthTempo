# YouthTempo 唯一路线图

更新时间：2026-09-19

本文件是唯一优先级与完成状态来源。当前结论：**READY WITH CONDITIONS**；代码和自动化准入已通过，真实学生入组前仍须关闭下列人工/外部条件。

## TEACHER REVIEW REALIGNMENT / 会前版本方向调整（2026-09-19）

本节记录张老师意见后的新开发范围。原有家长访问、专业支持和解忧室草案中的相反决定以本节为准；各项只有在代码、权限、测试和所需人工证据齐备后才能勾选。正式站及正式 Supabase 本轮不变更。原结论 **READY WITH CONDITIONS** 不因本地构建或演示通过而升级。

- [ ] 当前无家长试点的服务端和 RLS 数据边界：历史 active 关系不授予 SWEET、消息或支持数据访问；18 岁以上不进入监护关系；注销可处理历史 active 关系且留审计。
- [ ] 14–17 岁与 18–25 岁分流工作台和服务端资格：前者以家校医社支持、审核社区、自我记录为主；后者以受邀成年人主题聊天室、自我记录和真人支持为主。
- [ ] SWEET 作为非诊断性自我观察；轻量记录可日用、完整 SWEET 建议每周但随时可做；提醒偏好可关闭、不惩罚漏记。
- [ ] 青序花园第一版：只按参与成长，复用轻量记录与 SWEET 事实，私有、双语、移动和减少动态效果可用。
- [ ] 解忧室成年人邀请制主题群聊：服务端和 Realtime 资格、成员期历史隔离、值班、举报、屏蔽、静音、删除、人工审核与高风险升级。
- [ ] 四类支持人员分权：专业咨询人员、社工、倾听志愿者、学校值班老师；申请材料私有、人工审核、授权范围和审核日志明确。此项取代此前“不设普通倾听志愿者”的旧决定。
- [ ] 未成年人支持请求与人工评估、转介、连续性；成年人正式咨询的内部测试流程、主要及后备支持者、交接和撤权；公开正式咨询默认关闭。
- [ ] 轻量、可跳过且每七天最多提示一次的试点反馈及受限后台汇总。
- [ ] 隔离数据库中的 Migration/RLS、年龄与角色、浏览器双语移动、类型与构建验证；正式 Migration 另待批准。
- [ ] 会前演示文档、生产部署运行手册、分主题提交及远端功能分支推送。不得合并或部署。

**待张老师决定：** 专业人员体验反馈、专业审核和督导的权重；暂停/退出标准；是否公开评价；正式咨询开放时间；未成年人未来选择性家长分享；真实值班与危机升级负责人。当前不实现公开排行、自动淘汰、自动分配或支付。

**本分支验证状态：** 已编写上述模块的本地代码、迁移草案、双语文案和会前文档；`pnpm typecheck`、`pnpm build` 与无凭据浏览器测试已通过。隔离 PostgreSQL/Supabase 未能启动（Docker 镜像下载的 DNS 失败），所以 Migration 执行、RLS、Storage、旧 session、四类角色和咨询完整生命周期尚未验收；这些复选框继续开放。正式环境未变更，当前结论仍是 **READY WITH CONDITIONS**。

**远端交付状态：** 主题提交保存在本地功能分支；2026-09-19 推送 `origin` 遇 GitHub 443 连接超时，远端分支未确认创建。网络恢复后须单独重试推送并核对远端 HEAD；这项交付保持未完成。

## COMPLETED / 已完成

- [x] SWEET 五维记录、AI 小结、保存、历史详情与删除；Sleep→Wake 手机滚动回归已覆盖。
- [x] Mood Journal、Worry Time 与 Referral；五个支持入口均在任何普通生成前执行统一中英文确定性危机识别。青少年 AI 对话“陪我捋一捋”已于 2026-08-18 对首轮学校试点关闭；Referral 与 Worry Time 均已改为版本化固定规则，普通路径不调用 provider。
- [x] SWEET Talk 家长沟通工具，明确基于 AIDET，且不是 SWEET 第六维。
- [x] Email OTP 八位验证码、持久 session、安全错误提示和重发冷却；当前真实多邮箱复验见人工项。
- [x] 学生、监护人、老师、学校负责人、平台管理员、专业支持者角色与服务端权限边界。
- [x] 老师—学生、监护人—孩子关系分配；成员/学校退出后关系与旧会话撤销。
- [x] 社区可见范围、评论、举报、`safety_review`、审核审计、屏蔽、禁言与处理 SLA。
- [x] 学生同意与监护人确认、撤回、账号导出/注销，以及账号注销审计和不含学生内容的学校退出操作记录 24 个月到期清理；这不代表 SWEET、留言、社区或其他学生正文统一保存 24 个月。监护人仍关联 active `student_guardian` 同意时的注销冲突见下方 PILOT BLOCKER。
- [x] 角色、学校、同意、关系和验证状态均由服务端数据库事实决定；RLS、触发器和列权限作为纵深防护。
- [x] AI、社区、消息、反馈、举报、专业认证等高滥用入口限流；API 5xx 和监控事件脱敏。
- [x] 中英文核心流程、响应式 320–1440 px、基础无障碍、移动浏览器自动化与安全响应头。
- [x] Supabase 版本迁移、25 张业务表 RLS、空库 baseline 恢复验证；依赖审计无已知漏洞。
- [x] Next.js standalone Docker、腾讯云香港 Lighthouse、Nginx/HTTPS 和正式域名部署架构；GitHub Actions 仅在 `main` 的 Verify 全部通过后使用受限 SSH 密钥自动部署，并带候选容器检查和失败回滚。
- [x] 微信小程序一期工程：OTP、同意、SWEET、AI 小结、本人历史读取/删除和支持入口。
- [x] 2026-09-04 关闭正式 Supabase 的 E2E fixture 污染：GitHub Verify 已移除正式项目地址和正式 `service_role` secret，正式/恢复项目由 fixture 脚本与 Playwright 双重拒绝，公开 OTP 登录入口不会为保留的 `@youthtempo.test` 测试域创建新用户；正式库中 10 个固定账号的 session、refresh token、Auth 用户、资料、2 所学校、关系、同意、SWEET 记录、认证、邮箱引用与 Storage 所有权已逐项只读核对为 0。
- [x] 2026-09-07 统一 SWEET 每日记录规则与本人删除入口：以上海日历日为准，每人每天仅最新一条记录有效；保存新记录后清理本人当天旧记录，账户历史、管理员最近记录和奖励核对统计均只展示/计算每日最新记录；删除有效记录时一并清理本人该日遗留重复项；平台管理员可在账户页查看并删除自己的记录，但不能删除学生记录。
- [x] 2026-09-07 增加奖励核对用的学生 SWEET 参与统计：按既有学校、老师分配权限展示近 7 天和近 28 天记录日数、当前连续日数、最长连续日数及最近记录时间；同一天多次记录只计一天。统计仅作为人工核对依据，不自动决定现金奖励，不加入排名、断签惩罚或重度游戏化。
- [x] 2026-09-08 根据首条 SWEET 填写反馈完成全部 16 道选项题审计并落实首轮调整：五个必答主问题统一为单一维度、五级或明确时长区间；补充题删除重复的晨间启动与饮食因果提问，合并活动类型，修正时长端点，并让“不太确定”与具体多选原因互斥。Web 中英文与小程序主问题保持一致，历史记录中的旧答案仍由规则兼容识别；该调整不是临床量表修订。
- [x] 2026-09-13 修订确认首批专业支持产品基线：现有“支持老师”定位为班主任、导师等“学生支持老师”；新增目标身份“校内心理老师”；外部支持由 YouthTempo 直接招募并严格审核符合资质的个人专业志愿者，机构合作降为未来可选项。“志愿”只描述服务方式，不降低资质要求，也不设置无专业资质的普通志愿倾听者类别。平台招募、个人专业资质与具体学生授权必须分离，专业资质不再作为排他的全局角色；平台不保存完整咨询、诊断或治疗病历。完整决定见 `PROFESSIONAL_SUPPORT_MODEL_DECISION.md`，本项只代表产品模型已确认，不代表代码、数据库或真实专业服务已完成。
- [x] 2026-09-13 确认入口边界：现有“悄悄话信箱”继续用于学生向已关联老师、家长或自己表达不便当面说的话，不作为对接心理支持的入口。未来专业支持使用独立入口和独立授权关系，不把学生留言自动转为专业支持事项；既有确定性高风险识别与安全升级责任不因此弱化。
- [x] 2026-09-13 确认年龄与家长方向：当前试点继续不要求家长加入、不创建监护关系；18 岁及以上用户不进入家长加入流程，未满 18 岁的家长可选加入留待后续政策、同意和权限设计批准后实施。
- [x] 2026-09-13 完成个人专业身份申请的首轮本地改造：中英文申请表和平台审核台均将机构/单位改为选填，服务端把空机构规范化为 `null`，审核通过仍强制要求专业方向、资质类型、资质编号、发证/登记机构和核验材料；迁移 `20260912161714_allow_individual_professional_applicants.sql` 移除机构作为 active 状态前提并保持函数仅向 `service_role` 开放。针对性测试 20 passed，翻译键一致性 2 passed，`pnpm typecheck` 与 `pnpm build` 通过。迁移尚未应用到正式 Supabase，未开放任何学生数据或专业支持服务。
- [x] 2026-09-13 完成当前无家长试点的应用层收口：用集中策略开关关闭后台家长账号、新增亲子关系、批量名单家长字段、家长账户记录加载和学生“写给家长”入口；服务端同时拒绝家长成员创建、关系新增及家长收件请求，并保留空名单解除旧关系的清理路径。18 岁及以上年龄判断由服务端同意记录决定，不信任客户端选择；学校运营完整度不再把家长覆盖率当作缺口。未来 14–17 岁家长功能代码保留但默认关闭。策略、名单、运营与中英文专项回归 17 passed，`pnpm typecheck` 与 `pnpm build` 通过。正式 Supabase 未被连接；本轮未生成或应用数据库迁移，旧 active 关系的 RLS 收紧见下方开放项。
- [x] 2026-09-13 完成学生端首轮年龄分流说明：账户页依据服务端返回的同意年龄分组，为 14–17 岁显示自主试用、已分配老师与当前无家长收件说明，为 18 岁及以上显示独立使用、校园/社区/合格专业支持路径；悄悄话信箱继续是表达入口，不宣称进入咨询。大学生公开页新增“按个人基线判断”的说明，明确晚睡、晚起、外卖或不固定作息本身不能推出心理异常，应结合是否偏离本人常态及持续功能影响。顶部身份入口、SWEET 五维、社区名称、功能权限和数据库均未改变。年龄分流、消息边界、双语键与公开页回归 21 passed，`pnpm typecheck` 与 `pnpm build` 通过。
- [x] 2026-09-13 确认“解忧室”产品与技术方向：公开名称固定为“解忧室”，内部可暂用 `peer_space`；它是独立的学生表达与同伴倾听空间，不由家校医社区直接改名，也不复用悄悄话信箱或专业支持入口。底层预留 `18_plus` 与 `14_17` 两个服务端隔离空间，成年人邀请制试点先行；14–17 岁当前保持 `hidden`，界面不显示入口、“暂未开放”卡片或候补名单。完整决定见 `PEER_SPACE_PRODUCT_DECISION.md`；本项只代表方向已确认，不代表页面、数据库、权限或真实服务已实现。
- [x] 2026-09-13 确认成年人解忧室实时群聊用户流程与权限矩阵 `PEER_SPACE_ADULT_MVP_DESIGN_DRAFT.md`：首批面向完成 `adult_self` 确认并获得试点邀请的成年学生；采用有人值守的持久化文字群聊。规则确认后自动加入唯一的跨校“大家的解忧室”，cohort 只管理邀请和运营；公共房间可选择匿名、房间昵称或经验证姓名，学校标签默认隐藏且由本人自主展示。首期不创建、不自动加入或显示本校聊天室，相关方案移入 `PEER_SPACE_FUTURE_UPGRADES.md`。经验证姓名必须来自可信服务端来源，不得回退到可编辑 `profiles.display_name` 或邮箱；学校不获得学生使用统计。现有安全、举报、限流与审计机制可以复用，但不能直接复用现有社区的帖子/评论、角色受众、真实姓名/邮箱显示或管理员全量读取模型。本项只代表产品基线已确认，不代表技术或运营审批完成。
- [x] 2026-09-13 修订 `PEER_SPACE_TECHNICAL_DESIGN_DRAFT.md`：独立 `peer_space_*` 数据域改为 space、cohort、membership、公共 room、room membership、房间身份、持久化 message、屏蔽、举报、排班、审核、安全事件和身份映射审计；形成 Pages Router API 发送前安全链路、私有 Realtime 最小事件、普通业务表 deny-all、Realtime 授权桥接、身份与学校标签动态解析、断线游标补齐、旧连接撤销和允许/拒绝测试矩阵，并明确学校认证不得创建隐藏的本校 room、membership 或 topic。草案未创建 migration、未修改 schema/RLS、未连接正式 Supabase，也未编写功能代码。
- [x] 2026-09-13 完成 `PEER_SPACE_STUDENT_UX_DESIGN_DRAFT.md`：沿用现有成年人功能卡片和 YouthTempo 视觉语言，定义资格隐藏、首次告知、自动加入公共房间、匿名/昵称/经验证姓名、学校展示二次确认、交流期待、实时消息、滚动、断线、只读/暂停、删除/屏蔽/举报/退出、通知隐私、无障碍和中英文验收标准。首期不显示本校聊天室或未来房间占位；解忧室卡片只对服务端确认的受邀成年学生显示，不改顶部身份入口、中学生页、家长页、家校医社区或悄悄话信箱。本项为 UX 草案，不代表页面实现或生产开放。
- [x] 2026-09-13 完成 `PEER_SPACE_STAFF_UX_DESIGN_DRAFT.md`：对照现有试点值班、社区审核和管理后台，设计独立的 capability 工作台，覆盖排班签到、开放前核对、开房/只读/暂停、实时房间维护、官方提示、举报领取与并发保护、限时禁言/移出、高风险安全事项、渐进身份查看、交接、无人接班自动只读、配置管理、异常降级和审计。即使同一专职人员兼任多项职责，也须切换当前能力并分别授权/审计；本项为 UX 草案，不授予任何真实数据访问权。
- [x] 2026-09-13 确认解忧室数据与运营参数：普通消息不设自动到期，学生历史按每一段有效 room membership 隔离，新成员不能回看加入前内容、重新加入不能读取离开期间内容；本人可随时删除，普通删除正文 24 小时内清理，必要举报/安全证据结案后最多 180 天，去标识化操作审计最多 24 个月。单条消息 600 字符；每个开放房间至少 1 名当班和 1 名独立备班/安全联系人，30 秒续租、60 秒提醒、120 秒自动只读；正文 API 撤权立即生效、旧 Realtime 最多 5 分钟停止；7 天禁言/移出和常规身份查看实行双人复核。学校仅获得房间配置和值班准备等纯运营状态，不获得学生人数、活跃度、消息、主题、举报或其他使用统计。本项仅更新设计与治理文档，不批准数据库、代码或生产变更。
- [x] 2026-09-13 确认解忧室经验证姓名边界：首批只接受带确认人、学校、来源、时间和更正/撤销审计的学校名单确认记录，不收身份证件；当前 `profiles.display_name`、邮箱、Auth metadata 和缺少独立确认记录的邀请姓名均不能直接作为实名。姓名工作流未完成前实名全局关闭；失效或姓名变化时公共房间回退昵称，重新确认后由学生再次选择。本项仅更新设计文档，不批准 schema、migration 或正式名单处理。
- [x] 2026-09-13 将“本校解忧室”延后为未来功能升级并建立 `PEER_SPACE_FUTURE_UPGRADES.md`：当前只实施跨校公共聊天室，不创建或预建本校 room、membership、Realtime topic，不显示入口、“暂未开放”、申请或候补；公共房间仍可由学生自主展示验证学校。本项仅更新设计与治理文档。
- [x] 2026-09-14 完成解忧室首个非生产界面批次：用仅开发/测试环境可启用、生产强制关闭的服务端预览开关，在成年人入口动态显示卡片，并提供首次规则告知及唯一公共聊天室的加载、开放、只读、暂停、不可用页面状态；输入与发送能力保持禁用，未接数据库、账号资格、消息 API 或 Realtime，也未改变中学生、家长、顶部身份入口、家校医社区或悄悄话信箱。默认关闭回归 1 passed；本地预览桌面/移动回归 6 passed；中英文框架回归 5 passed；`pnpm typecheck` 与两次 `pnpm build` 通过。第二次生产构建即使显式传入 `PEER_SPACE_UI_PREVIEW_ENABLED=true`，中英文路由仍进入 `notFoundRoutes`。
- [x] 2026-09-15 完成解忧室第二个非生产界面批次：公共聊天室内可在当前页面切换系统房间昵称与稳定匿名标识，并选择或清除“只想说出来、希望有人听见、想听相似经历、可以给建议、想了解支持资源”等交流期待；顶部身份摘要同步更新。经验证姓名与学校展示保持禁用并说明可信名单、审计、默认隐藏和不收身份证件的边界。选择在刷新/离开后恢复默认，不写入浏览器存储、数据库或 API，消息发送仍禁用，入口范围未扩大。另按设计规范移除成年人入口卡片中用 CSS 模拟的聊天气泡装饰。首次回归发现视觉隐藏单选框可能被固定导航遮挡，已改为可见单选圆点并增加键盘焦点环；修正后本地预览桌面/移动 8 passed，默认关闭与中英文框架 6 passed，`pnpm typecheck`、生产构建和浏览器视觉核对通过；即使生产构建显式传入预览变量，中英文 `/peer-space` 仍进入 `notFoundRoutes`。
- [x] 2026-09-15 完成解忧室第三个非生产界面批次的纯视觉整理：压缩房间顶部层级并统一状态、身份摘要、入口与设置卡片的间距、边框和色彩；空房间减少无意义留白，增加“想说一点 / 先安静看看”双向引导但不伪造在线人数、消息或同伴回应；设置卡片强化选中、禁用、键盘焦点和移动端排列，禁用输入区在窄屏改为纵向布局。本轮未改变状态机、数据边界、发送能力、入口范围或生产隐藏规则。`pnpm typecheck`、`pnpm build` 与 `git diff --check` 通过；预览桌面/移动 8 passed、默认生产隐藏与中英文框架 6 passed；浏览器核对默认房间、展开设置、选中和禁用状态正常。生产构建即使传入预览变量，中英文 `/peer-space` 仍进入 `notFoundRoutes`，生产包实测路由 404 且成年人入口不出现解忧室文案。
- [x] 2026-09-15 完成解忧室工作人员端首个非生产界面批次：新增仅开发/测试显式启用、生产强制 404 的 `/admin/peer-space` 工作台预览；展示示例能力上下文、今日班次、备班与安全联系人准备状态、唯一公共房间状态、开放前核对清单，以及只在当前页面生效的签到、开放、转只读、暂停、交接和重置演示。所有联系人、数量和服务状态均明确标注为占位；未接入现有管理权限、真实账号、排班、学生内容、举报队列、数据库、API、Realtime 或通知，生产 `/admin` 未增加入口。工作人员端桌面/移动交互 4 passed，既有学生端联合回归 8 passed，双语框架 5 passed，生产默认隐藏 2 passed；`pnpm typecheck`、生产构建和浏览器默认/签到/开放状态核对通过。生产构建即使传入预览变量，中英文 `/admin/peer-space` 与 `/peer-space` 仍全部进入 `notFoundRoutes`。
- [x] 2026-09-16 精简解忧室工作人员端非生产预览为“当前任务优先”模式：压缩顶部说明，按未签到、当班和交接阶段只突出当前任务；把五条签到核对归并为工作环境、人员与安全路径、规则与交接三组；签到后自动收起准备内容并把房间控制前置；零待办改为单一空状态，关键服务按需展开，操作结果就近反馈；区分开放主操作、只读/交接次操作和需要二次确认的暂停风险操作。未改变功能边界、生产隐藏或接入真实数据与服务。`pnpm typecheck`、生产构建和 `git diff --check` 通过；预览学生端、工作人员端与中英文框架 11 passed、2 skipped，生产默认隐藏 2 passed、6 skipped；浏览器实操核对签到后的定位与暂停二次确认正常，中英文学生端及工作人员端生产路由继续进入 `notFoundRoutes`。
- [x] 2026-09-16 精简解忧室学生端非生产预览的进入路径：把受邀成年人解忧室入口提前到成年人页面主视觉之后；将首次告知归并为实时与值班边界、身份与可见范围、隐私与安全三组；同意进入后自动定位房间标题；收敛房间空状态；身份设置只突出可用选项，并将尚未开放的经验证姓名和学校展示收进折叠说明。保留首次确认、消息保存与成员期、安全复核、生产隐藏和现有数据边界，未接入真实消息或服务。`pnpm typecheck`、生产构建和 `git diff --check` 通过；学生端、工作人员端与中英文框架联合回归 11 passed、2 skipped，最终学生端回归 4 passed、1 skipped；浏览器实操核对入口顺序、三组告知、进入后标题焦点和按需展开正常。生产构建中英文学生端与工作人员端路由继续进入 `notFoundRoutes`，成年人静态页面不含解忧室入口文案。

- [x] 2026-09-17 完成解忧室首批本地授权基础：使用 Supabase CLI 创建尚未应用的 `20260917011148_add_peer_space_access_foundation.sql`，并同步合并 schema；新增成年人 space、试点 cohort、邀请 membership、不可变规则确认事件、唯一跨校公共 room 和按时段保存的 room membership。普通客户端对六张业务表保持 deny-all，仅服务端获得显式最小 grant；`18_plus`、当前 `adult_self`、学生角色和 active 邀请必须同时成立，规则确认通过服务端专用原子函数激活 membership 并幂等加入公共房间。新增 `/api/peer-space/access` 与 `/api/peer-space/rules`，并以服务端 `PEER_SPACE_ACCESS_API_ENABLED` 开关默认关闭，不接受客户端 user、school、cohort 或 room 授权事实。公共房间初始为 `closed`；未创建消息、Realtime、工作人员能力、本校房间或 `14_17` 空间实例，学生端与工作人员端生产入口继续强制 404。成年人/未成年人/未受邀/家长/老师/伪造用户资格、迁移边界与未登录 API 专项 12 passed；生产包默认隐藏 4 passed、12 skipped by design；`pnpm typecheck`、`pnpm build` 与 `git diff --check` 通过。本地没有可用 Supabase/Postgres 运行时，迁移只完成静态一致性核对，未连接或应用任何远程项目。
- [x] 2026-09-17 完成解忧室工作人员授权与值班状态的第二批本地基础：补完尚未应用的 `20260917020225_add_peer_space_staff_duty_foundation.sql` 并同步 consolidated schema；新增独立的 `room_duty`、`content_moderator`、`safety_duty`、`config_admin` capability 及 space/room scope、有效期、不可恢复撤销和自动授权审计，不能从平台管理员、学校成员、专业身份或 `profiles.role` 推断。值班表要求当班与独立备班/安全联系人，签到保存五项当前版本确认但不自动开房；30 秒心跳使用 120 秒服务端租约，签到、续租、交接、结束和租约失效通过仅 `service_role` 可执行的原子函数追加审计，失效回收函数会在没有另一有效班次时把已开放房间降为只读。新增默认关闭的 `PEER_SPACE_STAFF_API_ENABLED`、最小工作人员上下文 API 和签到/心跳/交接/结束 API；不返回其他工作人员账号或联系方式。四张业务表均启用 RLS、普通客户端 deny-all；未创建消息、Realtime、审核/安全队列、排班配置 UI、生产人员或真实班次，也未开放生产工作台。专项桌面/移动回归 12 passed，`pnpm typecheck`、`pnpm build` 与 `git diff --check` 通过。本机没有 Supabase CLI/Postgres 运行时，migration 尚未执行数据库级语法、RLS/advisor 或允许/拒绝查询；未连接或修改任何远程 Supabase。60 秒内部提醒、持续调度租约回收、真实排班授权和生产运维验收仍须在后续批次完成。
- [x] 2026-09-19 完成解忧室房间状态控制的本地小阶段：用 Supabase CLI 创建未应用的 `20260919065043_add_peer_space_room_state_controls.sql` 并同步 consolidated schema；增加仅服务端可写的状态审计和默认关闭的 `/api/admin/peer-space/rooms`。开房必须由当前 `room_duty` 当班人员操作，原子确认有效租约、独立且有效的备班/安全能力、五项签到和成年人空间状态；只读/暂停记录固定原因码，暂停只能由 `safety_duty` 先解除到只读，再由有效当班另行开房。未创建消息或 Realtime，预览界面未连接 API，生产入口仍隐藏。相关桌面/移动回归 32 passed，类型与构建通过；本地 Docker daemon 不可用，新增 SQL 尚未执行数据库级查询。60 秒提醒、持续回收调度、真实排班配置与人员、正式迁移和运营验收均未完成，READY WITH CONDITIONS 不变。

## PILOT BLOCKERS / 真实学生入组前必须关闭

- [ ] 修复监护人注销与 active `student_guardian` 同意的数据库冲突：2026-09-04 清理 E2E 账号时确认，直接删除监护人会把 `student_consents.guardian_user_id` 设为 `NULL`，继而违反 `student_consents_active_basis_check` 并使 Auth 删除返回 500。当前 fixture 清理已通过“先学生、后监护人”避免该冲突，但真实监护人自助注销前必须明确同意应转为 withdrawn、pending 或其他状态的产品/隐私规则，再实施服务端事务和允许/拒绝回归；未经该决定不得修改 schema 或静默保留 active 状态。
- [ ] 落实 2026-08-28 已批准的学生自主试用同意政策：当前无学校、老师或监护人参与的试用仅接收 14 岁及以上学生；14–17 岁学生完成本人敏感信息与 AI 处理单独确认后，可生成 AI 小结并保存本人记录。不得伪造监护关系；没有有效学校、老师或监护人关系时，对方不得获得访问权。代码、版本化 `student_self_pilot` 依据、双语文案与不依赖远程数据库的回归已完成；正式 Supabase 迁移 `20260827174237_enable_student_self_pilot_consent` 已于 2026-08-28 应用并完成只读结构核对。真实保存、撤回和再次阻断的生产生命周期尚未获得单独测试授权，因此本项保持开放。正式学校试点前必须启用新的学校同意版本并重新审核适用的学生与监护人确认，不得沿用自主试用依据自动放行。
- [ ] 完成 AI P0 安全整改：五个自由文本支持入口的统一确定性危机识别、Talk 全会话扫描、固定现实支持指引和自动化回归已于 2026-08-18 完成；“我觉得活着没有意义”“活着还有什么意思”及对应英文被动轻生表达已纳入确定性规则，并补充中英文正例与课程/作文讨论反例。技术整改已完成，仍需安全/心理专业负责人审核词表、固定文案和学校线下承接流程，形成签字证据后关闭。
- [ ] 完成 AI P0 产品与治理整改：青少年自由 AI 对话 Talk 已关闭，Referral 与 Worry Time 已改为固定规则；Check-in 与 Mood Journal 的模型职责进一步收敛为“只选择来源字段 ID”，服务端严格校验 ID 后用固定模板组成小结，模型不能直接写入最终展示文本；工具/行动/支持内容由规则或用户原文决定。生成 API 已绑定服务端身份与有效同意，并增加生成总开关、provider 主机与模型白名单及姓名/学校/班级/地址/证件/联系方式等常见标识移除。2026-09-07 经试点发起人明确授权，正式环境实际使用的 `api-router.fly.dev` 与 `claude-haiku-4-5` 已加入显式白名单；候选容器及切换后正式容器均以无用户数据的合成请求获得 HTTP 200，公网健康检查正常。首批试点继续开放受限 AI 小结，可用 `AI_GENERATION_ENABLED=false` 一键关闭；该技术恢复不替代供应商保留/训练/处理地点和跨境影响评估。日期模型快照、生成记录最小化 provenance（需另行批准数据库变更），以及法务、隐私、心理专业和学校告知签字作为后续治理事项继续完成。
- [ ] 配置自有 SMTP、SPF/DKIM/DMARC，并当次实测 QQ、163、Outlook 收件、八位 OTP、session 持久化与安全失败提示。
- [ ] 完成备份恢复闭环：加密异地真实备份、隔离数据库恢复、校验和、数据库 RTO 24 秒、恢复关系 JWT/RLS 验收、账号删除重放、香港每日加密任务和 Mac 异地同步已完成；仍需真实邮箱 OTP/session 的隔离应用验收、连续周期观察、外部失败告警和完整业务 RPO/RTO。禁止在生产库恢复。
- [ ] 配置正式告警接收端，并分别制造登录、保存、AI、社区四类受控失败，确认 Lighthouse、Nginx、容器日志和告警均无正文、邮箱、token 或验证码。
- [ ] 启用学生自主试用值班链路：2026-08-28 已确认由试点发起人承担首位值班负责人；学生主动联系、无学校高风险留言入队、平台管理员处理审计，以及只含事件编号/时间/后台链接的 SMTP 邮件提醒已实现。正式 Supabase 迁移 `20260827200529_add_pilot_duty_message_queue` 与外键索引补充迁移 `20260827200713_index_pilot_duty_foreign_keys` 已于 2026-08-28 应用，迁移历史与安全/性能 advisors 已复核；香港环境 SMTP/收件地址尚未配置，也未完成虚拟留言的真实收件与失败重试验收。启用前须登记值班时段、预计首次查看时间、替补联系人和非值班时段线下路径；邮件不得包含学生身份或正文，学生端不得承诺实时回复。
- [ ] 正式学校试点或开放监护人访问前，由产品/学校/隐私负责人签署监护人访问政策：当前技术边界为“经学校核验的 active 监护关系可看孩子完整 SWEET 记录和小结”，并确认同意文案与学校告知一致。当前学生自主试用不创建关系，本项不阻止无关系的学生本人功能测试。
- [ ] 在首批校内心理老师或平台专业志愿者获得任何学生数据前，落实 `PROFESSIONAL_SUPPORT_MODEL_DECISION.md`：区分学生支持老师、校内心理老师和平台直接招募的合格个人专业志愿者；将学校岗位、平台招募状态、个人专业资质与具体学生授权拆分；建立个人申请审核、获准服务范围、独立专业支持入口、明确接收、访问到期与撤销审计。完成中英文界面、允许/拒绝路径、跨校/跨人员隔离、旧 session 失权及到期回归前，专业志愿者不得作为普通学校成员获得学生访问权。本项不阻止没有专业人员数据访问的纯学生自主试用，也不改变悄悄话信箱的校内表达用途。
- [ ] 审批 `PROFESSIONAL_SUPPORT_TECHNICAL_DESIGN_DRAFT.md` 中的数据库关系、API 权限和后台入口。产品方向已确认由 YouthTempo 直接招募符合资质的个人专业志愿者、机构合作仅为未来可选项；个人申请的机构选填已完成本地代码、迁移和静态回归，但迁移尚未应用到正式 Supabase。仍需专业服务、隐私/安全和适用学校负责人确认资质标准、服务范围、连续性、未成年人规则和责任边界；本地实现不等于生产实施授权。
- [ ] 在开发独立专业支持入口前确定最小学生流程：学生主动选择寻求专业支持、查看谁能看到什么、提交最少信息、看到承接状态与预计响应时间，并可撤回、结束或申请换人。该流程不得复用或替代悄悄话信箱，也不得要求先完成 SWEET。
- [ ] 在正式纳入大学试点前完成年龄路由：应用层已关闭当前家长流程，并阻止 18 岁及以上用户新增关系或向家长收件；账户与公开页已完成首轮年龄分流和个人基线说明。正式开放前仍须先清点并解除任何历史 active 成人监护关系，并经单独迁移批准把同样年龄/同意边界加入 RLS，完成直接客户端允许/拒绝回归。未满 18 岁家长可选加入功能继续关闭，直至产品、学校和隐私规则另行批准。大学生 SWEET 各题的解释规则和真实校园支持资源仍须与大学试点方独立验证，不能把本轮文案调整视为完成成人适用性验证。
- [ ] 在真实 iPhone Safari、安卓 Chrome、微信内置浏览器各完成 OTP、同意、SWEET 保存/历史/删除、退出登录。
- [ ] 学校确认危机升级联系人、值班时段、举报 2/24/72 小时处理责任人和线下应急路径。学生自主试用的平台值班链路不能自动替代未来学校试点的校内职责。

## PILOT MANUAL VERIFICATION / 开放前人工核对

- [ ] 正式域名与香港 Lighthouse 已绑定；仍需确定试点期是否保持 `noindex`，并据此复核 canonical、sitemap、robots 及适用备案/合规事项。
- [ ] 在 Vercel 账户侧断开旧 GitHub 自动部署集成，确认香港 Lighthouse 的 `Deploy Production` 是唯一正式自动部署目标。
- [ ] 在独立 E2E Supabase 项目运行虚拟学校 fixture 后完成全角色允许/拒绝矩阵，并留存当次测试报告；不得使用正式或恢复演练项目。
- [ ] 在独立 E2E Supabase 项目使用两所虚拟学校检查跨校隔离、监护关系撤销、老师取消分配、学校退出和旧 session。
- [ ] 由学校核对家长告知书、学生同意、隐私说明、数据保留/删除和退出试点流程。
- [ ] 开放前复核 `PILOT_MANUAL_CHECKLIST.md` 与 `SCHOOL_PILOT_READINESS_CHECKLIST.md` 全部必选项。

## POST-PILOT / 试点后

- [ ] 继续完成并审批 `PEER_SPACE_PRODUCT_DECISION.md` 的阶段 1：成年人邀请制、有人值守的实时文字群聊、规则确认后自动加入唯一的跨校“大家的解忧室”、公共房间匿名/昵称/经验证姓名、学校名单确认实名与安全回退、学校标签由学生默认隐藏并自主展示、交流期待、无自动到期且按成员期隔离的历史、职责分离、具体值班/心跳/旧连接/复核和学校无学生使用统计均已确认；首期不实现本校聊天室。学生端状态与交互见 `PEER_SPACE_STUDENT_UX_DESIGN_DRAFT.md`，工作人员端值班/审核/安全/配置流程见 `PEER_SPACE_STAFF_UX_DESIGN_DRAFT.md`，数据库、Pages Router API、持久化消息、私有 Realtime 最小事件、RLS/授权桥接和测试矩阵见 `PEER_SPACE_TECHNICAL_DESIGN_DRAFT.md`。本地授权基础已完成但 migration 尚未应用；仍须审批两份 UX 草案，并决定学校名单确认界面的操作者分工、批量更正与学生异议处理时限、实际开放时段、规则更新确认方式及 5 分钟 Realtime 失权的实现选型；后续 migration、正式数据库应用和真实服务开放仍须分别获得技术、隐私/安全、审核运营及大学试点方的对应审批。开放前值班人员必须已经到岗并完成开房、普通举报、安全事项、身份查看和交接演练；`14_17` 继续完全隐藏，未来开放须作为新的产品、安全、学校和权限决定单独批准。
- [ ] 在提交或应用本地解忧室值班 migration 前完成单房间并发/交接规则与数据库级验证：本地暂按交接流程约束每房间最多一条 `active` 和一条 `handoff_pending`，租约到期或主动结束时仅在无另一条有效班次时降为只读；签到、续租、交接和失效回收统一先锁房间、再锁班次，并在加锁后重新检查状态。migration、consolidated schema 和静态回归已同步。2026-09-19 在无网络、无挂载的临时本地 PostgreSQL 17 容器中，完整 consolidated schema 加载通过（仅为镜像缺失的 `auth.jwt()` 添加容器内测试桩）；合成数据事务验证交接重叠、第二条 active 被拒绝、旧班过期不关闭有新班覆盖的房间、最后一班结束转只读、续租与撤权后拒绝续租，且浏览器角色无值班表/RPC grant、直接查询被拒。两会话并发签到观察到数据库锁等待，首班提交后第二班被唯一约束拒绝。合成数据均不含真实用户，未连接远程 Supabase。此证据不等于 migration 链独立重放、Supabase advisors、完整 RLS 允许/拒绝矩阵、交接与回收同时竞争、撤权后的房间状态与内容鉴权收敛、调度延迟或真实 120 秒降级验收；仍须复核排班业务决定并在后续独立环境补齐。此项属于未来成年人解忧室开放条件，不改变当前学生自主试用的 READY WITH CONDITIONS 结论。
- [ ] 审核 2026-09-19 并行加入的本地房间状态控制批次：`20260919065043_add_peer_space_room_state_controls.sql`、`/api/admin/peer-space/rooms` 和专项测试已出现在工作树并追加至 consolidated schema，但加入时间晚于上方隔离 PostgreSQL 验证，不能沿用该执行结果。提交前须单独验证开房主备班/签到边界、只读与暂停/恢复授权、审计、并发与浏览器角色拒绝路径，保持默认关闭和生产不开放；正式 Supabase 未应用。
- [ ] 在成年人解忧室真实开放前落实持续值班回收和内部提醒：先在独立测试数据库核对 Cron 可用版本、执行身份、运行记录、失败告警和轮询延迟，再经单独批准调度现有 `expire_peer_space_duty_leases()`；60 秒无心跳提醒须明确接收人、渠道、去重、重试及不含青年身份或正文的最小内容。当前只有未应用的回收函数，没有定时任务或已送达提醒；不得启用正式任务，也不得承诺恰在第 120 秒转只读。
- [ ] 公共聊天室试点稳定并完成证据复盘后，再按 `PEER_SPACE_FUTURE_UPGRADES.md` 重新评估本校聊天室；不得因学校认证、人数增长或合作需求自动创建，须重新获得产品、技术、隐私/安全、审核运营和大学试点方批准。
- [ ] 根据学生、家长、老师反馈评估 SWEET 问题长度、家长访问粒度和学校工作台信息密度。
- [ ] 为 CI 增加真正的 ESLint/静态规范检查；当前 `lint` 脚本仅提示未配置。
- [ ] 基于匿名化运营指标复盘参与率、退出率、安全审核时效和支持转介完成度。
- [ ] 完成微信 AppID、合法域名、隐私指引、体验版真机验收和平台审核后再发布小程序。
- [ ] 优化首屏关键插图加载优先级，消除当前构建/浏览器的 LCP 图片提示。
- [ ] 评估危机检测对第三人称引用、课程讨论和玩笑语境的误报；任何调整必须保持高风险召回并经安全负责人批准。

## LONG-TERM / 长期方向

- [ ] 建立可持续事务邮件服务、自动加密备份、定期恢复演练和告警值班制度。
- [ ] 正式网站与小程序统一发布、隐私版本、账号身份和审计流程。
- [ ] 在证据支持下扩展学校合作、专业支持网络和 18–25 岁场景；不以诊断或治疗为产品承诺。
- [ ] 校园推广、教师培训、家长沟通、反馈收集、学生隐私教育与安全事件复盘形成标准运营包。

## 明确不做

- 新增第六个 SWEET 维度；SWEET Talk 是基于 AIDET 的家长沟通工具。
- 排名、惩罚性连续打卡、重度游戏化或未经审核的大规模公开社区。
- 在核心网页与安全运营未稳定前扩张功能或用户规模。
