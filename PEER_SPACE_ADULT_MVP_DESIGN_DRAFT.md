# 成年人解忧室实时群聊用户流程与权限矩阵

> **2026-09-19 新方向提示：** 本文保留早期决策/设计脉络；与 `ROADMAP.md` 会前版本方向冲突的唯一公共房间、可展示经验证姓名、无普通倾听志愿者或尚未实现的描述均以新路线图和 `docs/TEACHER_REVIEW_READINESS.md` 为准。代码仍处开发分支，正式环境未开放。

状态：**成年人准入、仅公共聊天室、有人值守的实时群聊、三种房间身份、学校自主展示、交流期待、历史范围、值班参数和职责分离已于 2026-09-13 确认为产品基线；本校聊天室移入未来升级。2026-09-17 已有非生产页面预览，以及尚未应用的本地成年人准入和工作人员值班 migration、默认关闭的 API；这些只覆盖基础，不代表完整实时群聊已实现。后续消息、Realtime、运营验收、正式迁移和真实用户开放仍须分别批准。**

关联决定：`PEER_SPACE_PRODUCT_DECISION.md`

本基线只设计 `18_plus` 成年人邀请制解忧室。`14_17` 仍为 `hidden`，不显示入口或占位，也不允许通过直接 URL、旧客户端或 API 访问。

## 1. 产品目标

让参加试点的成年学生能够：

1. 在清楚公共房间范围、值班状态和隐私边界后，选择匿名、房间昵称或经验证姓名参与实时文字群聊。
2. 完成解忧室规则确认后自动加入并进入“大家的解忧室”；首期不创建或展示本校聊天室。
3. 在大家房间中自行隐藏或展示经过验证的学校标签，不因试点批次自动暴露学校。
4. 用交流期待告诉别人此刻更需要倾听、相似经历、温和建议还是支持资源。
5. 删除本人消息、屏蔽成员、举报内容或退出解忧室。
6. 在明确危险内容发出前先进入现实支持和人工安全流程，而不是先广播给其他学生。

解忧室不承担咨询预约、诊断、治疗、紧急服务、交友或社交关系沉淀。实时表示消息能够即时到达房间，不表示咨询师、老师或任何工作人员必然立即回复。

## 2. 首批范围

### 包含

- 18 岁及以上、完成 `adult_self` 有效确认并获得试点邀请的学生。
- 有人值守的纯文字实时群聊。
- 跨学校的“大家的解忧室”。
- 公共聊天室中的可选验证学校标签；本校聊天室不在首期范围，未来升级见 `PEER_SPACE_FUTURE_UPGRADES.md`。
- 公共房间的系统匿名标识、房间昵称和经验证姓名。
- 大家房间中默认隐藏、可由学生主动开启的验证学校标签。
- 持久化聊天历史、断线重连、缺失消息补齐和发送状态。
- 交流期待、本人删除、举报、屏蔽、禁言、移出、房间只读与暂停。
- 确定性安全检查、人工审核队列、值班交接和审核审计。
- 独立支持资源入口，但不自动创建专业支持事项。

### 不包含

- 14–17 岁用户或任何跨年龄互动。
- 图片、音视频、附件、外链预览、地理位置或直播。
- 一对一私信、加好友、关注、粉丝、随机配对或联系方式交换。
- 精确在线成员名单、个人在线状态、已读名单或“谁正在输入”。
- 点赞数、热榜、排行、连续签到、创作者等级或公开发言数量。
- 院系、年级、班级、专业等更细身份标签。
- 家长、老师、学校负责人或专业人员按原有角色进入学生聊天室。
- AI 对话、AI 自动回复、AI 诊断或 AI 最终审核决定。

## 3. 四个不能混淆的概念

| 概念 | 决定什么 | 不决定什么 |
|---|---|---|
| 试点批次 `cohort` | 谁被邀请、试点开始结束时间、运营归属 | 不自动决定学生能看见哪个聊天室 |
| 聊天室 `room` | 消息由哪些学生实时接收和读取 | 不自动公开学生所属学校 |
| 房间身份模式 | 在当前房间对其他学生显示匿名标识、昵称或经验证姓名 | 不改变内部账号映射、举报、屏蔽或安全授权 |
| 学校展示偏好 | 大家房间中是否显示本人经验证的学校标签 | 不改变房间成员和消息可见范围 |

首批只提供一个跨学校“大家的解忧室”。多个获批试点批次的成年学生进入同一个公共聊天室；学校认证只可用于本人自愿展示学校和经验证姓名，不创建额外聊天室或内容范围。

## 4. 准入判定

每次进入、读取历史和发送消息都由服务端判断：

1. Supabase session 对应的当前用户有效。
2. 当前资料角色为学生；其他角色不因成年、学校关系或专业资质获得学生入口。
3. 当前有效同意记录为 `age_band = '18_plus'`、`consent_basis = 'adult_self'`。
4. 当前用户至少有一个有效试点 membership。
5. `18_plus` 空间和目标房间处于允许状态。
6. 公共房间 membership 有效。
7. 用户未被移出；被禁言者可以读取本人获准历史、删除、举报、屏蔽和退出，但不能发送。

任一条件不满足，应在返回房间、历史或成员信息前停止。未成年人、未受邀用户和其他角色通过直接 URL 访问时，返回不泄露房间存在或成员情况的通用不可用结果。

### 管理员按登录邮箱预邀（本地实现，正式未开放）

首期由平台管理员收集学生将用于登录的邮箱，依据可信名单或其他可复核来源确认成年试点资格，选择试点批次，学校可留空。管理员记录最少必要的资格来源、核验人与时间；邮箱本身、邮箱域名、可编辑资料和 `school_members` 关系均不能证明成年或大学生身份。没有大学学籍依据时，后台只标“受邀成年试点成员”，不标“已核验大学生”。学校留空的成员可使用跨校公共房间，但没有可展示的验证学校或经验证姓名。

管理员预邀先保存为待认领邮箱邀请，不预先确认 Auth 邮箱，也不代学生填写 `adult_self`。学生用同一邮箱完成 OTP 登录并亲自完成当前成年同意后，服务端核对有效邀请并绑定当前用户，创建 `invited` membership；首次进入还需主动确认当前解忧室规则，随后自动加入公共房间。后续登录只需服务端资格仍有效，无需逐次人工审批。房间是否可以发言继续取决于实际值班和房间状态。

平台管理员工作台提供邮箱预邀、可选学校、资格来源、批次、待认领/已认领/已激活/已撤销状态，以及更正和撤销操作；学校可选字段不自动创建学校成员关系或向学校披露解忧室参与情况。申请入口留待未来决定。当前 `school_invites` 要求学校且服务于学校成员关系，`peer_space_memberships` 要求已有 `auth.users`；独立邮箱邀请表、服务端认领和撤销审计已在本地实现。正式数据库迁移、开关启用和真实用户开放仍须单独批准与验收。

首次接受解忧室规则时，服务端自动创建“大家的解忧室” room membership，`visible_from` 为确认成功时间。学校认证建立或变化不创建其他 room membership。学生曾主动退出公共房间时记录 auto-join 抑制，不因刷新、重新登录或规则重复同步而静默加回；只有本人明确重新加入才建立新成员期。

## 5. 首次进入流程

### 5.1 入口

- 首批入口只显示在符合资格的 18 岁以上学生工作台，名称固定为“解忧室”。
- 副标题可表达“有人值守时，可以实时说说最近压在心里的事，也可以安静听听别人的经历”。
- 不使用“咨询”“治疗”“专业解答”“随时有人回复”或“24 小时在线”，除非相应能力已真实建立并经过验收。
- 未受邀成年人不显示入口；`14_17` 不显示入口、灰色卡片或“暂未开放”。

### 5.2 首次边界告知

进入公共聊天室前至少说明：

- 大家房间由获准的成年学生跨学校共同参与；首期没有本校聊天室或学校范围信息流。
- 公共房间默认展示房间昵称；本人可切换为系统匿名标识或经验证姓名。匿名只针对其他学生，平台在获准安全流程中仍可追溯内部账号。
- 消息会保存并经过安全检查；明确危险内容可能不会公开，而是进入人工安全复核。
- 消息不设自动到期，但本人只能查看自己每一段有效房间成员期内产生的消息；后来加入者看不到加入前的聊天，退出或被移出期间的消息也不会在重新加入后补发。
- 履职所需的值班与审核人员可能看到必要消息，但普通内容不会自动发送给家长、老师、学校或专业人员。
- 解忧室不是紧急服务；存在立即危险时应联系现实支持或当地紧急服务。
- 不交换联系方式、不私聊、不发布能识别自己或他人的信息。
- 用户可以删除本人消息、屏蔽、举报或退出；已经展示的信息仍可能被他人看到或截图。

用户主动确认当前规则版本。规则确认只用于解忧室，不能替代 `adult_self` 同意，也不能扩张其他数据用途。

## 6. 公共聊天室

### 6.1 进入与状态

首期不需要让学生在多个聊天室之间选择。`/peer-space` 根据资格和规则状态进入“大家的解忧室”或其值班状态页；可用紧凑入口卡显示名称、跨学校范围和值班状态，例如“值班开放”“暂时只读”“已暂停”，不显示成员名单、精确在线人数或发言热度。

本校聊天室和主题房间不显示卡片、灰色占位、“暂未开放”、申请或候补。未来升级见 `PEER_SPACE_FUTURE_UPGRADES.md`。

### 6.2 大家的解忧室

- 多个大学或邀请来源的成年学生可在同一房间交流。
- 不提供按学校筛选信息、成员搜索或学校排行榜。
- 学校标签默认隐藏；学生可在房间设置中主动开启或关闭。
- 展示的学校必须来自服务端验证关系，不能由用户自由填写。

### 6.3 暂缓的本校聊天室

首期不创建本校房间、成员关系或 Realtime topic，也不在学生端显示任何入口。此前关于按验证学校隔离、自动加入、历史成员期、独立身份设置和值班边界的设计作为未来升级保存在 `PEER_SPACE_FUTURE_UPGRADES.md`，不能被当前实现提前带入。

## 7. 房间身份与学校展示

### 7.1 三种房间身份

| 模式 | 对其他学生显示 | 规则 |
|---|---|---|
| 系统匿名标识 | 例如“匿名同学·7K3” | 每段房间成员期随机生成，用于区分对话者；不跨房间复用，不代表对平台匿名 |
| 房间昵称 | 审核过的中性词库昵称 | 默认模式；按房间隔离，可从有限安全候选中更换 |
| 经验证姓名 | 可信来源确认的姓名 | 仅本人主动选择并二次确认；当前可编辑 `profiles.display_name` 不能直接作为已验证姓名 |

- 首期只保存公共房间的当前身份模式；数据结构可支持未来按房间隔离，但不得据此预建本校房间。
- 首批不允许每条消息单独切换身份，避免对话关系、举报和屏蔽产生混淆。
- 身份模式是动态展示设置，不写入每条消息的公开快照。切换后，该房间当前及历史消息按新模式渲染；从经验证姓名切回匿名或昵称不能收回他人已经看到或截图的信息。
- 三种模式内部均绑定同一不可公开的 room membership；举报、屏蔽、禁言和安全审计不会因对外身份变化失效。
- 匿名标识和房间昵称按成员期及房间隔离；未来若增加其他房间，不得复用公共房间的稳定标识。

### 7.2 经验证姓名来源与失效

- 首批唯一可接受来源是学校通过受信任名单流程确认的学生姓名。确认记录须独立保存姓名、用户、学校、来源名单、确认人、确认时间、状态以及后续更正或撤销；仅把姓名写入 `profiles.display_name` 或 Auth metadata 不构成验证。
- 现有 `school_invites.display_name` 可以作为学校名单流程的输入，但因为当前结构缺少独立的姓名确认状态和完整更正审计，不能直接作为实名展示依据。
- 首批不要求学生上传身份证、学生证或人脸材料，也不以邮箱前缀推断姓名。无学校确认记录的成年学生仍可使用匿名和房间昵称，但经验证姓名不可用。
- 姓名确认功能、隐私告知和允许/拒绝测试完成前，经验证姓名选项全局关闭；完成后按账号返回是否可用，不因学校认证自动公开。
- 确认记录被撤销、过期、进入更正，或确认姓名发生变化时，公共房间立即回退为房间昵称；当前和历史消息同步更新，不向其他学生发送“姓名失效”通知。
- 重新确认后不自动恢复经验证姓名。学生须在公共房间重新选择，并二次确认将公开的准确姓名。
- 学校只负责确认名单事实，不得获知学生是否选择实名、在哪个房间发言或说了什么。

### 7.3 学校展示偏好

- 只适用于“大家的解忧室”，默认值为“隐藏我的学校”。
- 开启前明确提示：“其他人会在你当前的房间身份旁看到学校名称，也可能据此认出你；请不要再透露院系、年级、班级或联系方式。”
- 展示粒度只到学校名称，不显示院系、年级、班级、专业或试点批次。
- 学校名称不写入单条消息快照；界面按当前偏好渲染。关闭后，本平台界面中的当前和既往消息都不再显示学校标签。
- 关闭不能撤回他人已经看到、记住或截取的信息，告知文案必须说明这一点。
- 修改学校展示不改变公共房间范围，不创建或转入本校聊天室，也不向学校发送通知。

## 8. 实时交流流程

### 8.1 选择交流期待

学生进入房间后可以选择或更新当前交流期待：

| 期待 | 房间中的提示 | 其他成员边界 |
|---|---|---|
| 只想说出来 | “此刻更想被安静听见” | 不追问、不分析；可以用轻量的“我听见了”回应 |
| 希望有人听见 | “欢迎理解和陪伴” | 回应感受，不急着指导 |
| 想听相似经历 | “可以分享自己的经历” | 不替对方下结论，不比较谁更严重 |
| 可以给我一些建议 | “可以给温和、可选择的建议” | 不命令、不诊断、不保证结果 |
| 想了解支持资源 | “更需要可靠的下一步” | 房间提供独立资源入口；不自动转介或发送正文 |

交流期待是当前会话身份旁的提示，不要求附在每条消息上，也不限制学生在对话中更新。它不能改变安全规则或给其他人专业处置权。

### 8.2 发送消息

1. 客户端显示“发送中”，先把消息交给 Pages Router API，而不是直接广播。
2. 服务端重新验证年龄、同意、空间、房间、membership、禁言状态和限速。
3. 运行中英文确定性危机识别，再检查明显辱骂、威胁、联系方式和高风险个人信息。
4. 普通消息持久化成功后，才向该房间的获准实时订阅者发送最小事件。
5. 客户端收到确认后显示“已发送”；超时或失败显示“发送失败，可重试”，不能假装成功。
6. 明确危险内容停止普通发送，展示固定现实支持指引并进入 `safety_review`；在人工决定前不向其他学生广播。

首批单条消息建议上限 600 个字符，不支持附件、链接、提及、引用转发或嵌套线程。原异步方案中“首帖 2,000 字、回应 600 字”的区分不再适用于实时群聊；最终长度仍须结合移动端和审核演练确认。

首批不把完整消息发送给第三方生成式 AI。未来加入 AI 预审核须另行审批供应商、告知、保留、训练、处理地点、误伤与人工复核；AI 只能辅助排序或提示，不能诊断或作最终处置。

### 8.3 历史与断线恢复

- 消息历史以业务数据库为准，不依赖短期 WebSocket 重放作为唯一记录。
- 普通消息不设自动到期时间；只要账号、房间和相应成员期仍满足当前授权，学生可持续查看该成员期内产生且未被删除、移除或屏蔽的消息。
- 历史下界和上界来自每一段 room membership 的 `visible_from` 与 `visible_until`。后来加入者看不到加入前的消息；主动退出、被移出或资格撤销结束当前可见区间，重新加入只建立新的区间，不补发离开期间的消息。
- 断网、关闭页面和退出登录不结束 active room membership，因此不会缩短既有历史范围。
- 首次进入定位到最新可见消息，初始加载 50 条，向上滚动每次再加载 50 条；不提供全局搜索、按成员查找或跨成员期浏览。
- 断线时显示“正在重新连接”，暂停发送，恢复后按最后确认游标补齐缺失消息并去重。
- 被删除、移除或屏蔽的消息在补齐时仍须遵守当前状态，不能因离线缓存重新出现。
- 房间从开放转为只读或暂停时，现有客户端立即更新状态并停止新的发送入口。

## 9. 值班与房间状态

| 状态 | 学生体验 | 发送规则 |
|---|---|---|
| `staffed_open` | 显示“值班开放”，可读取和实时发送 | 允许，仍受个人禁言和安全规则限制 |
| `read_only` | 可查看获准历史，显示下一次计划开放时间 | 服务端拒绝新消息 |
| `paused` | 显示简短安全或运营提示 | 停止普通历史和发送，保留本人删除、举报及支持资源路径 |
| `closed` | 房间不再开放 | 不可读写，按保留政策处理历史 |

- `staffed_open` 必须有 1 名已到岗的当班人员、1 名独立备班/安全联系人、交接记录和可用安全升级路径；首批默认每名当班人员只负责 1 个开放房间。
- 当班租约每 30 秒续期；连续 60 秒未收到有效心跳时提醒当班和备班，连续 120 秒没有任何有效当班租约时由服务端自动转为 `read_only`。
- 系统可以预先实现全天排班能力，但不能在人员尚未到岗时对用户显示 24 小时开放。
- “值班人员”是运营职责，不等同于心理咨询师；只有具备独立专业服务资格和学生授权时，才可通过专业支持入口开展相应服务。
- 值班人员可以使用清晰的官方房间身份发布规则提醒或秩序提示，但不能伪装成普通学生。

## 10. 删除、屏蔽、举报与退出

### 删除

- 用户可以删除自己的消息，普通界面立即停止展示。
- 不设单独的限时“撤回”功能；本人删除随时可用。无未结事项时，删除正文应在 24 小时内完成物理清理。
- 若存在未结举报或安全事件，只保留处理所必需的最小副本；结案后最多保留 180 天。不含正文、明文邮箱或用户编号的操作审计最多保留 24 个月。
- 平台移除须记录原因、操作者、前后状态和时间，不能用硬删除掩盖审核行为。

### 屏蔽

- 屏蔽后双方在共享房间历史和后续实时消息中互相不可见，且不能通过旧链接继续定位互动。
- 被屏蔽者不会收到“谁屏蔽了我”的通知。
- 屏蔽不影响平台对已举报或高风险内容的审核。

### 举报

- 举报后目标消息立即对举报者隐藏。
- 举报人可查看本人举报状态，但不能看到审核人员身份或他人处置细节。
- 2/24/72 小时目标只有在真实排班和升级路径登记后才能向用户承诺。

### 退出

- 退出解忧室不影响 YouthTempo 账户和其他本人功能。
- 退出后立即停止新的 API 读取和发送，并取消房间订阅；消息不因退出自动删除，但退出者在没有 active membership 时不能继续读取。
- 再次加入仍须满足年龄、同意和邀请条件，只建立新的历史可见区间，不恢复离开期间的消息权限。主动退出标记会阻止相同条件下的自动加回，直到本人明确重新加入。

## 11. 权限矩阵

“否”表示服务端拒绝，不是只隐藏按钮。

| 能力 | 未登录/未受邀/14–17 岁 | 受邀成年学生 | 家长/老师/学校/专业身份 | 内容审核员 | 安全值班负责人 | 平台配置管理员 |
|---|---|---|---|---|---|---|
| 看到学生入口 | 否 | 是 | 否 | 仅工作入口 | 仅工作入口 | 仅配置入口 |
| 进入大家房间 | 否 | 是，active membership | 否 | 仅获派工作视图 | 仅事件所需 | 否 |
| 进入本校房间 | 否 | 首期禁止；无入口、room membership 或 topic | 否 | 否 | 否 | 否 |
| 读取聊天历史/实时接收 | 否 | 仅获准房间、仅本人有效成员期 | 否 | 仅获派范围 | 仅安全事件所需 | 否 |
| 发送消息 | 否 | 是，房间开放且未禁言 | 否 | 仅官方秩序消息需独立授权 | 仅官方安全提示需独立授权 | 否 |
| 展示/隐藏学校 | 否 | 仅大家房间、仅本人 | 否 | 否 | 否 | 否 |
| 选择房间身份 | 否 | 公共房间选择匿名、昵称或经验证姓名；实名仅在 active 学校名单确认存在时可选 | 否 | 否 | 否 | 否 |
| 删除本人消息、屏蔽或举报 | 否 | 是 | 否 | 审核员不以成员身份操作 | 否 | 否 |
| 查看普通举报队列 | 否 | 否 | 否 | 仅获派范围 | 除非同时获派 | 否 |
| 查看高风险安全队列 | 否 | 否 | 否 | 仅分级所需摘要 | 仅获派范围 | 否 |
| 禁言、移出、暂停房间 | 否 | 否 | 否 | 按获批能力和范围 | 仅安全处置范围 | 配置管理员可改房间状态但不读正文 |
| 查看账号—房间身份映射 | 否 | 仅本人 | 否 | 否 | 仅 active 安全事件、原则上双人批准并审计 | 否 |
| 管理邀请、房间和排班 | 否 | 否 | 学校仅按批准流程提供认证事实和运营联系人 | 否 | 否 | 是，但不因此读取正文 |
| 查看学校运营数据 | 否 | 仅本人状态 | 仅房间是否配置、值班条件是否具备；无学生使用统计 | 仅工作量 | 仅安全事件指标 | 仅管理纯运营状态 |

## 12. 审核与值班职责

### 房间值班人员

- 负责房间开放、秩序提示、举报初看、交接和无人值守时转为只读。
- 使用明确的官方身份，不使用学生昵称。
- 默认看不到账号—房间身份映射，也不因值班身份成为专业咨询师。

### 内容审核员

- 查看获派房间中的待审核消息、举报和必要上下文。
- 恢复、移除、禁言或解除限制，每次操作填写原因并记录审计。
- 可单独作出 1 小时或 24 小时禁言；7 天禁言或移出须第二人批准，等待期间最多先执行 24 小时临时限制，学生可在处置后 7 天内申诉一次。
- 默认看不到真实姓名、邮箱、学校关系和账号—房间身份映射。

### 安全值班负责人

- 只处理确定性高风险或人工升级事件。
- 先查看消息和最少上下文；只有现实安全处置确有必要时，才通过受限操作查看账号映射。
- 查看映射原则上须两名 `safety_duty` 人员批准，并记录事件、理由、查看人、复核人和时间；存在迫切现实安全风险时允许一人先查看，但须在 24 小时内补做第二人复核。
- 不把普通情绪表达自动转交学校、家长或专业人员。

### 平台配置管理员

- 管理年龄空间、公共房间状态、试点邀请、排班和工作人员能力；首期不提供学校房间配置。
- 默认不读取消息正文、不处理普通举报、不查看账号—房间身份映射。
- 同一人兼任多项职责时，仍按当前操作能力授权和审计，不能用单一管理员身份获得全部读取权。

## 13. 可复用与不可直接复用的现有能力

### 可以复用

- `lib/messageSafety.ts` 与 `lib/safety/crisisDetection.ts` 的确定性中英文安全路径。
- 社区举报分类、优先级思路、屏蔽、禁言和人工处理审计模式。
- 普通 authenticated 客户端不能直接访问敏感业务表的纵深防护方式。
- 用户安全错误、运营日志脱敏和旧 session 失权测试模式。

### 不能直接照搬

- 现有社区按角色和学校开放；解忧室必须按年龄、有效邀请和房间 membership 授权。
- 现有社区作者可能回退到真实姓名或邮箱；解忧室只能返回本人当前房间身份模式允许的匿名标识、昵称或经验证姓名，以及本人主动选择的学校标签，绝不回退到邮箱。
- 现有社区是帖子/评论模型；解忧室是持久化的实时房间消息模型。
- 现有平台管理员可处理全部社区内容；解忧室必须拆分房间值班、内容审核、安全身份查看和配置能力。
- 当前试点值班留言队列是非实时求助承接，不能直接冒充实时聊天室排班系统。
