# YouthTempo Pilot Readiness Audit

> **2026-09-19 会前版本说明：** 本功能分支的目标是当前试点完全不开放家长数据访问，历史 active 关系不能授权读取；成年人按独立路径运行。旧清单或审计中关于监护人可查看学生完整记录、解忧室只是预览、无普通倾听志愿者的描述仅保留为历史事实，不代表新目标或正式环境已更新。详见 `ROADMAP.md`、`docs/TEACHER_REVIEW_READINESS.md` 与部署手册。正式结论仍为 **READY WITH CONDITIONS**。

> **2026-09-20 MVP 隔离进度：** 一次性本地 Supabase 已完成第 55 份迁移升级、历史 active 关系下 guardian 旧 session 失权、学生本人读取、目标关系修复 dry-run 回滚、guardian/学生注销、花园/反馈和默认关闭入口专项；定向浏览器 63 passed、0 failed。此前较广范围的角色、Storage、支持与聊天室验证不作为本批阻断门槛。不得据此关闭人工或学校 PILOT BLOCKER，整体仍为 **READY WITH CONDITIONS**；详见 `ROADMAP.md`。

审计日期：2026-08-07；AI P0 增量复核：2026-08-18；学生自主试用政策变更：2026-08-28；专业支持模型与解忧室方向修订：2026-09-13；当前工作树交付复核：2026-09-19
结论：**READY WITH CONDITIONS**

## 基线

- 审计起点：`main` / `5e993f124e13f4344ff0922bef5e0cafc242954a`，当时本地落后 `origin/main` 1 个提交。
- 审计前工作区已有用户修改：`README.md` 与未跟踪的 `AGENTS.md`；本次保留并纳入最终文档更新。
- Node `24.18.0`，pnpm `11.16.0`；项目声明 Node `>=20.9 <27`、pnpm `11.9.0`。
- Next.js `16.2.12` Pages Router、React `19.2.8`、TypeScript 5、Supabase JS `2.50.0`。
- 初始审计基线为 42 个 migration、26 张业务表；当时 schema 中 Data API 业务表均启用 RLS。此处不是当前工作树计数。
- 2026-09-19 会前功能分支静态计数（含尚未应用的本地 migration）：52 个 migration SQL、consolidated schema 中 55 张 public 表及 55 处 RLS 启用、63 个 Playwright spec。计数不证明 migration 已执行或 RLS 行为已验收。
- 当前正式部署目标：香港腾讯云 Lighthouse 上的 standalone Docker，经 Nginx 提供 `https://youthtempo.com`；Vercel 与原 CloudBase 均不是正式站。

## 发现与修复

| 级别 | 发现 | 处理 |
|---|---|---|
| P0 | 多个 API 的 500 分支可能把数据库/提供商 `Error.message` 返回客户端 | 已统一为用户安全通用错误，同时保留脱敏服务端监控；新增静态防回归测试 |
| P1 | Messages、反馈、举报、专业认证缺少共享原子限流 | 已补齐按用户/动作限流和安全的 429/503 响应 |
| P1 | AIDET 家长工具命名未落实最终产品决定 | 已更名 SWEET Talk，并明确“基于 AIDET、不是第六维” |
| P1 | 真实社区测试依赖 fixture 残留状态 | 关键测试脚本现先重置两所虚拟学校 fixture |
| P0 | GitHub Verify 曾直接连接正式 Supabase 并在主分支推送时重建 10 个固定 E2E 账号及关系数据 | 2026-09-04 改为仅接受独立 E2E 项目专用 secrets；正式/恢复项目增加代码级拒绝，CI 退出时强制清理；GitHub Actions 正式 `service_role` secret 已移除；公开 OTP 登录不会为 `@youthtempo.test` 创建新用户。正式库 10 个账号、2 所学校及关联数据已删除，Auth、业务表、邮箱引用和 Storage 所有权逐项核对为 0 |
| P0 | 删除仍关联 active `student_guardian` 同意的监护人会触发 `student_consents_active_basis_check`，导致 Auth 删除返回 500 | fixture 清理已固定为学生优先、监护人最后并增加回归；真实监护人注销需要产品/隐私先决定关联学生同意的状态迁移，再实现原子服务端处理，本项保持 PILOT BLOCKER |
| P1 | 用户内容/私密入口缺少明确搜索引擎策略 | 已加页面 `noindex` 及 `robots.txt`；canonical/sitemap 等待正式域名 |
| P1 | AI 指令未显式声明用户输入不是系统指令 | 已增加中英文提示注入边界并测试 |
| P0 | 原先只有 Talk 最新一条消息进入确定性危机识别，其余四个自由文本 AI 工具可能先调用 provider；Talk 的早先高风险消息也可能被后续消息冲掉 | 五个支持入口现均在 provider 调用前执行共享中英文确定性识别；Talk 扫描本次会话全部用户消息；命中后只返回固定现实支持指引。Talk 普通生成已于 2026-08-18 对首轮学校试点关闭，兼容 API 仍保留危机优先路径。专业审核和学校线下承接仍是开放条件 |
| P0 | 生成式工具缺少一致的事前确认、身份/同意绑定和持续 AI 标识 | 当前只有 Check-in、Mood Journal 两个网页工具及小程序 Check-in 调用模型，且只生成记录小结；使用 `ai-notice-2026-08-18-v2`，普通请求须通过服务端登录、有效同意与版本校验，结果持续显示“AI 辅助记录小结·非评估或诊断”。Worry Time 与 Referral 使用固定规则，Talk 已关闭。固定危机路径优先且不调用 provider；provider 数据政策与告知仍待法务、隐私、专业及学校签字 |
| P0 | 模型可直接写最终小结，来源字段、provider 与模型版本未被严格约束 | 模型现只返回最多 2/3 个来源字段 ID；服务器拒绝未知、重复、超量或额外字段，并使用经脱敏的原文片段和固定模板组成最终小结。首批试点保持受限生成开启，仍须通过 HTTPS、provider 主机与模型白名单校验，并可用环境总开关立即暂停。日期快照、供应商尽调、跨境评估与数据库 provenance 作为后续治理条件继续完成 |
| P0 | “我觉得活着没有意义”等被动轻生表达可能绕过确定性危机识别 | 已加入中英文被动轻生正例和“哲学课/作文讨论生命意义”反例；五个支持入口均在普通处理前复用同一检测器，命中后停止处理并优先连接家长、老师或其他可信任成年人。中文区分 110/120 即时危险与 12356 非即时心理支持，英文只指向当地紧急服务；词表和文案仍待专业及学校签字 |
| P1 | 站内 AI 与紧急支持文案存在旧范围描述和求助顺序不一致 | 已完成中英文公开页、工具页、隐私页、社区、消息、小程序及项目说明的专项扫描；公开说明准确区分 AI 摘要、固定规则和关闭功能，现实支持顺序统一为家庭、学校、其他可信任成年人和专业/紧急支持，不把“家庭或学校不安全”作为默认或显眼叙事 |
| P0 | Talk 的开放式 AI 对话边界、专业监督与首轮试点必要性尚未完成证明 | 已选择首轮学校试点关闭：页面不提供输入或发送，普通 API 请求返回版本化 410 状态且不调用 provider；只为旧客户端保留确定性危机优先路径。重新开放须满足 `docs/TALK_PILOT_CLOSURE.md` 的审核条件 |
| P0 | Referral 原先把结构化问卷和自由补充文字发送给模型，由模型生成支持路径 | 已改为 `referral-rules-2026-08-18` 确定性规则；普通路径只读取结构化选项，补充文字仅经过本地危机检查，不发送给 provider、不评分、不诊断。持续/明显日常影响或用户偏好只用于升级真人支持；规则仍待心理专业及学校审核 |
| P1 | `lint` 仍是占位脚本 | 未修复；列入试点后维护项，当前由 TypeScript、build 和测试覆盖 |
| P2 | 首屏插图有 LCP eager 提示 | 未修复；不影响功能，列入试点后优化 |
| P2 | 危机词规则对第三人称引用/学术讨论可能保守误报 | 未弱化安全规则；列入安全负责人参与的后续调优 |
| P1 | 当前试用只有学生参与，14–17 岁学生本人确认后仍被学校核验监护人门槛阻止使用 AI 与保存 | 2026-08-28 已批准独立的学生自主试用依据：仅接收 14 岁及以上学生；14–17 岁完成本人敏感信息与 AI 处理单独确认后可使用本人功能。该依据不得伪装成监护人确认，不创建任何关系或额外读取权限；正式学校试点须启用新同意版本并重新审核。技术实现与验证结果见本次增量记录。 |
| P0 | 学生自主试用没有学校承接；高风险“写给自己”留言进入 `safety_review` 后没有明确真人接收人，现有提示可能让学生误以为已经送达真人 | 2026-08-28 已批准试点值班负责人方案。学生主动联系入口、无学校高风险留言值班队列、active 平台管理员后台、原子状态/处理审计和不含身份或正文的 SMTP 提醒已实现；两项正式 migration 已应用并复核 advisors，邮件失败不丢留言且不向学生声称已送达。香港 SMTP、真实收件、值班时段、替补与线下路径仍未完成，故保持阻塞。 |
| P0 | 当前“支持老师”同时覆盖班主任式日常跟进，系统没有独立校内心理老师岗位；专业支持者通过单一 `profiles.school_id` 挂靠学校，解除学校关系还会撤销全局专业核验，无法安全表达学校雇员与平台直接招募个人共同参与的专业支持 | 2026-09-13 已修订确认 `PROFESSIONAL_SUPPORT_MODEL_DECISION.md`：学生支持老师、校内心理老师、平台直接招募且符合资质的个人专业志愿者分层；平台招募关系、个人资质、获准服务范围和学生授权相互独立，机构合作仅为未来可选项。悄悄话信箱继续用于学生向老师、家长或自己留言，不作为专业支持入口；专业支持须使用独立入口和授权关系。除下列“机构选填”局部改造外，完整角色、服务范围、学生事项、同意文案及允许/拒绝测试仍未实现，因此在相关人员获得学生数据前保持 PILOT BLOCKER。 |
| P0 | 现有专业身份申请要求申请人填写所在机构，并把机构核验作为审核通过的数据库前提，不符合平台直接招募合格个人的已确认方向 | 2026-09-13 已在本地把机构改为选填，保留个人资质、编号、发证方和材料的必审要求；新增迁移、同步 schema、中英文界面、管理员审核和针对性回归。正式 Supabase 尚未应用迁移，完整的服务范围和学生事项授权也未实现，因此不改变 READY WITH CONDITIONS 结论。 |
| P0 | 当前后台仍可新增家长和亲子关系，“未关联家长”还会被算作试点缺口；18 岁以上虽使用成年人同意，应用层仍可能显示旧家长关系或允许向旧关联家长留言 | 2026-09-13 已用集中策略关闭当前试点的家长成员、新增关系、CSV 家长字段、账户关系返回和学生家长收件入口；关系 API 仍允许空名单解除旧关系，未来 14–17 岁代码保留但默认关闭。学校运营不再把家长覆盖计为完整度。服务端年龄来自 `student_consents`。本轮未获批数据库迁移，因此历史 active 成人关系的 RLS 失权仍是正式开放前条件。 |
| P1 | 中学生与成年大学生共用学生工作台，通用作息文案可能把大学阶段常见生活方式错误暗示为异常，也未清楚区分家长与独立使用路径 | 2026-09-13 已按服务端同意年龄分组显示 14–17 岁和 18 岁以上说明；大学生页面改为比较个人基线和持续功能影响，不凭晚睡、晚起、外卖或作息不固定单独下结论。未改评分或权限；成人 SWEET 解释规则及各校真实资源仍须试点验证。 |
| P1 | 若把现有多角色家校医社区直接改名为学生“解忧室”，会让学生误判可见范围，并把成年人和未成年人、同伴倾听与学校/家长协作混在一起 | 2026-09-13 已确认 `PEER_SPACE_PRODUCT_DECISION.md`：解忧室是独立、有人值守的成年人实时文字群聊；规则确认后自动加入唯一的跨校“大家的解忧室”。首期不创建、自动加入或显示本校聊天室，未来升级记录在 `PEER_SPACE_FUTURE_UPGRADES.md`。公共房间可选择匿名、房间昵称或经验证姓名；验证学校标签默认隐藏、由学生自主展示。14–17 岁完全隐藏，家校医社区、悄悄话信箱和专业支持入口继续保持各自边界。本轮仅修订产品和技术文档，未新增页面、数据库、权限或真实内容访问。 |
| P1 | 当前 `profiles.display_name` 可由学生本人修改，Auth metadata 同样不能证明真名；现有学校邀请姓名虽由名单流程输入，但缺少独立姓名确认状态、确认人和完整更正审计，不能安全支撑解忧室“经验证姓名” | 2026-09-13 已在产品、学生端、工作人员端、技术、权限和试点清单中确定：首批只接受独立的学校名单姓名确认记录，不收身份证件；实名工作流完成前全局关闭。确认失效或姓名变化时公共房间回退到昵称，当前与历史消息同步更新；重新确认后须由学生再次选择。当前未创建记录表、迁移、页面或正式名单流程，因此仍是实现前条件。 |

## 权限与数据结论

- 身份由 Supabase bearer token 服务端校验；角色、学校、分配、监护关系、同意与专业验证均从数据库加载，客户端字段不作为授权依据。
- 学生自主试用同意只放行学生本人的生成、保存、消息、社区与反馈入口，不会创建学校成员、老师分配或监护关系；所有第三方读取仍须原有 active 关系与 RLS 条件。
- 学生仅操作本人记录；老师仅看分配学生；学校负责人仅看本校；平台管理员走可信管理名单；专业支持者须通过且未过期。
- 当前没有独立的校内心理老师或平台专业志愿者服务权限。目标模型已确认，但在实现前，个人专业核验不得解释为学校成员、学生授权或平台值班权限；机构合作不是首批前置条件。
- 悄悄话信箱仍是学生给老师、家长或自己留言的独立功能，不是专业支持入口；除既有安全升级外，不得把留言自动交给专业人员。
- 当前学生自主试点不要求家长加入；应用层已统一关闭新增、展示和家长收件，18 岁及以上用户不适用监护人访问模型，未满 18 岁的家长可选加入仍待后续政策和实现批准。数据库仍须在单独批准后补齐历史关系的年龄/同意约束。
- 监护人当前只能在 active、学校核验关系下查看关联孩子，但可见的是完整 SWEET 记录与小结，不是仅摘要。该边界技术上已执行，政策仍须负责人签署。
- 社区按角色/学校/审核状态过滤；高风险内容进入 `safety_review`，普通用户不能自行发布待审核内容。
- 完整解忧室实时服务尚未实现，也不是当前家校医社区的别名。当前仅有生产强制隐藏的非生产界面预览，以及尚未应用的本地成年人准入和工作人员授权/值班租约基础；没有消息、Realtime、真实人员或生产排班。已确认首批为 `18_plus` 有人值守实时群聊：多个试点 cohort 进入唯一的跨校大家房间；首期不创建本校房间、成员关系或 Realtime topic。学校标签只在公共房间由本人默认隐藏并主动展示。消息不设自动到期但历史按有效 room membership 时段隔离，新成员不能回看加入前内容，重新加入不能读取离开期间内容。技术方案须把 `18_plus` 与 `14_17`、聊天正文与 Realtime 最小事件在服务端严格隔离；家长、老师、学校或专业身份不因关系或角色获得内容读取权。
- 账号注销、同意撤回、成员撤销、学校退出均有实际生命周期测试。2026-08-11 已在真实备份的隔离副本完成一次账号删除及幂等重放，profile/SWEET 级联删除、哈希审计和第二次零命中重放均通过。
- 2026-08-11 已完成一次正式库真实数据加密导出、双副本保存、服务器端校验、Mac 端实际解密/内部哈希验证及隔离项目数据库恢复；全程未在生产库执行恢复。数据库逻辑恢复耗时 24 秒，27 张 public 表均恢复并保持 RLS，核心关系断链校验通过；恢复关系上的学生、教师、监护人和校方管理员 JWT/RLS 边界验证通过。香港服务器每日加密备份已连续成功运行两次；第二个包也已通过同一 Mac LaunchAgent 流程同步并复核 SHA-256。验证后隔离项目的 Auth、public 和 Storage 业务数据均已清空。真实邮箱 OTP/session 的隔离应用验收、跨日连续周期观察和外部失败告警仍待完成。

## 已执行验证

- `pnpm audit --audit-level high`：无已知漏洞。
- `pnpm typecheck`：通过。
- `pnpm build`：通过，63 routes。
- `pnpm test:e2e`（最终完整回归）：275 passed，47 skipped by design（无 `.env.local` 注入的真实凭据生命周期用例按设计跳过；这些用例另以带环境脚本运行，结果见下项）。
- 权限/越权/撤销：16 passed，2 mobile duplicates skipped。
- SWEET 真实 AI 保存生命周期：1 passed，1 mobile duplicate skipped。
- 社区可见性与审核：fixture 重置后 2 passed，2 mobile duplicates skipped。
- 账号/同意/反馈/专业认证/角色保护/学校退出/小程序生命周期：7 passed，7 mobile duplicates skipped。
- 新增加固、320–1440 px 响应式、基础无障碍、Sleep→Wake 回归：7 passed。
- 移动 Chrome、WebKit/Safari 内核、微信 WebView UA：13 passed，2 项非微信项目按设计跳过。
- 2026-08-18 AI P0 增量：`pnpm typecheck`、`pnpm build` 通过；危机检测、AI API guardrails、双语 AI 合约及固定安全提示界面共 32 passed（desktop/mobile Chromium）。
- 2026-08-18 AI 透明度增量（关闭 Talk 前历史批次）：版本化事前告知、当时四个 AI 生成入口服务端校验、生成内容标识、小程序和公开同意说明共 48 passed（desktop/mobile Chromium）；当前普通网页 AI 生成入口为三个。
- 2026-08-18 Referral 去 AI 增量：四类规则分支、双语输出、无 AI 告知依赖、危机优先和页面“非 AI 生成”标识共 50 passed（desktop/mobile Chromium）；最终构建结果见本次交付记录。
- 2026-08-18 Talk 首轮关闭增量：版本化 410 关闭状态、普通请求零 provider 调用、旧客户端危机优先、页面无输入、替代支持入口、青少年入口移除及中英文/窄屏回归共 76 passed（desktop/mobile Chromium）；`pnpm typecheck` 与 `pnpm build` 通过。
- 2026-08-18 AI 范围收敛与全站安全文案增量：AI/API/危机/消息社区/双语文案 82 passed（desktop/mobile Chromium）；扩展规则、Talk 关闭、i18n、内容安全与试点加固批次 92 passed，另有 2 个旧文案断言按新“家长、老师或其他可信任成年人”要求更新后 Referral 批次 10 passed。最终 `pnpm typecheck` 与 `pnpm build` 通过，构建 63 routes。
- 2026-08-18 AI 来源约束与配置收口增量：模型只选来源字段 ID、服务器严格校验与固定模板、扩展标识移除、生成总开关、provider/model allowlist 及 B–G 越界输出拒绝共 90 passed（desktop/mobile Chromium）；`pnpm typecheck` 与 `pnpm build` 通过，构建 63 routes。未调用真实 provider，供应商后台与正式环境配置仍需人工验收。
- 2026-09-07 正式 AI 配置恢复：只读诊断确认失败来自实际 provider/model 未进入 allowlist，而非学生知情确认。经试点发起人明确授权后，将 `api-router.fly.dev` 与 `claude-haiku-4-5` 加入正式环境显式白名单；保留权限为 0600 的原配置备份。候选容器和切换后正式容器均通过无用户数据合成生成（HTTP 200 且返回内容），本机及公网健康检查正常，正式版本仍为 `dd15a339662ee12a511f07812d6b809baf2cff5c`。本项只证明技术链路恢复，不证明供应商数据保留、训练、处理地点或跨境条件已完成审核。
- 2026-08-28 学生自主试用同意增量：`pnpm typecheck` 与 `pnpm build` 通过；策略、迁移/schema 一致性、公开说明、小程序与双语安全文案专项 26 passed；完整不注入真实凭据的回归 347 passed、47 skipped by design。正式 Supabase 迁移 `20260827174237_enable_student_self_pilot_consent` 随后已应用并完成结构核对，但依赖真实生产数据写入的学生确认→AI→保存→读取→撤回生命周期没有获得单独授权，不能据此宣称该生产写入生命周期已实测。
- 2026-08-28 试点值班增量：学生主动联系、无学校 `safety_review` 入队、最小化邮件内容、active 平台管理员 API、处理审计和中英文非实时提示专项 90 passed；完整无真实凭据回归 357 passed、47 skipped by design；`pnpm typecheck` 与 `pnpm build` 通过。依赖审计发现的 `nanoid` 高危拒绝服务公告已用精确覆盖升级到 3.3.18，复验为无已知漏洞。正式迁移 `20260827200529` 与 `20260827200713` 已应用；安全 advisor 没有新增 ERROR，高风险外键无索引提示已消除。SMTP、真实收件和失败重试尚未执行，本项不能视为生产可用证据。
- 2026-09-13 无家长试点应用层收口：集中策略、服务端新增阻断、成年人独立路径、家长账户记录加载限制、CSV 模板、学校运营完整度及中英文结构专项 17 passed；`pnpm typecheck` 与 `pnpm build` 通过。本轮未连接正式 Supabase，也未生成或应用 RLS 迁移。
- 2026-09-13 学生端年龄分流说明：14–17 岁与 18 岁以上账户文案、悄悄话信箱收件范围、大学生个人基线说明、中英文键一致性及公开页面布局定向回归 21 passed；`pnpm typecheck` 与 `pnpm build` 通过。未连接正式 Supabase，未改权限或数据库。
- 2026-09-13 解忧室方向记录：固定公开名称、成年人先行、未成年人完全隐藏、年龄空间隔离及与现有社区/信箱/专业支持的边界已写入决策文档、路线图和人工核对项。本轮只有 Markdown 文档变更，因此未运行代码构建或端到端测试。
- 2026-09-13 成年人解忧室最小方案：已对照现有社区、安全、举报、屏蔽、禁言、限流和审核代码形成用户流程与权限矩阵草案。草案明确现有安全治理机制可复用，但角色受众、真实姓名/邮箱回退和平台管理员全量内容访问不能直接照搬。本轮没有页面、API、数据库、RLS 或生产变更。
- 2026-09-13 解忧室技术设计：在原异步帖子草案基础上按确认方向改为有人值守的持久化实时群聊；明确 cohort 只管理邀请和运营，首期只创建一个跨校公共 room。规则确认自动创建公共 room membership；学校验证不创建本校 room、membership 或 topic，只支持本人自愿学校标签和经验证姓名。Realtime 私有频道只传随机消息 ID 等最小失效事件，正文与当前对外身份均经逐次鉴权 API 返回；同时补充不存在本校隐藏预建、排班、只读/暂停、断线补齐、幂等发送和旧连接撤销测试。未创建 migration，未修改 schema/RLS，未连接正式 Supabase，未运行功能测试。
- 2026-09-13 解忧室学生端 UX 草案：对照现有 18–25 岁功能卡片、账号年龄入口、社区帖子流、悄悄话信箱和全站视觉样式，明确解忧室只在受邀成年学生状态下动态显示，不加入全站固定导航；形成首次告知、自动加入公共房间、房间身份设置、学校展示确认、交流期待、发送/断线/只读/暂停状态、举报屏蔽删除退出、通知隐私、无障碍和双语验收标准。本校聊天室和未来房间不显示入口、占位或候补。未新增路由、页面、字典键、插图或功能代码。
- 2026-09-13 解忧室工作人员端 UX 草案：对照当前 `PilotDutyMessageQueue`、`CommunityModerationQueue` 和长页面式 `/admin`，确认实时群聊不能继续使用平台管理员全量长页面；设计独立 capability 工作台，区分房间值班、内容审核、安全值班和配置管理，覆盖签到开房、只读/暂停、官方提示、举报原子领取、结构化处置、受限身份查看、交接、无人接班自动只读、通知最小化和异常降级。未新增路由、页面、API、权限、排班账号或真实内容访问。
- 2026-09-13 解忧室参数决定：普通消息不自动到期，学生只读本人有效成员期内产生的消息；普通删除 24 小时内清理，必要事项证据结案后最多 180 天，去标识化操作审计最多 24 个月。每房间至少 1 名当班和 1 名独立备班/安全联系人，30 秒续租、60 秒提醒、120 秒自动只读；旧 Realtime 最多 5 分钟停止，长期限制与常规身份查看双人复核。学校只获得房间配置、值班准备等纯运营状态，不获得学生人数、活跃度、消息、主题、举报或其他使用统计。本轮只有设计与治理文档变更，没有代码、schema、migration、生产连接或功能测试。
- 2026-09-13 解忧室经验证姓名决定：核对现有 schema、账号资料更新、学校邀请和名单分配流程后，确认当前没有可直接声明为“已验证真名”的独立事实。设计新增概念性 `peer_space_verified_names`，首批来源仅为带确认与审计的学校名单，不收证件；失效时回退昵称且重新确认不自动恢复实名。同步补充 API 最小化、允许/拒绝路径和学校不可见使用状态边界。本轮没有代码、schema、migration、生产连接或功能测试。
- 2026-09-13 解忧室首期范围收敛：只保留跨校“大家的解忧室”，本校聊天室整体移入 `PEER_SPACE_FUTURE_UPGRADES.md`。当前设计禁止创建或预建学校 room、membership 与 topic，也不显示“暂未开放”、申请或候补；公共房间的学校标签仍由学生默认隐藏并自主展示。同步更新路线图、权限矩阵和两份试点清单。本轮没有代码、schema、migration、生产连接或功能测试。
- 2026-09-14 解忧室首个界面批次：新增默认隐藏的成年人入口卡片、首次规则告知和唯一公共聊天室的加载、开放、只读、暂停与通用不可用状态。页面只在开发/测试环境显式开启预览，生产代码额外强制关闭；即使生产构建传入预览变量，中英文 `/peer-space` 仍列入 `notFoundRoutes`。本轮没有数据库、账号资格、消息 API、Realtime 或发送能力，不能视为解忧室已经实现或可开放。默认关闭、本地预览双端、中英文框架、类型与构建检查均通过。
- 2026-09-15 解忧室身份与交流期待预览：公共房间增加只在当前页面有效的系统昵称/稳定匿名标识和可选交流期待，顶部摘要随选择变化；经验证姓名与学校展示继续禁用，未读取账号姓名、学校或任何服务端数据。刷新后恢复默认，不使用浏览器存储、数据库或 API；发送能力仍关闭。桌面/移动交互、默认隐藏、双语、类型、生产构建与视觉核对通过，生产路由继续强制 404。本批次只用于确认隐私表达和交互，不改变 READY WITH CONDITIONS，也不能替代正式身份来源、资格、成员期、消息与安全权限实现。
- 2026-09-15 解忧室纯视觉整理：统一公共房间顶部、状态、身份摘要、设置卡片和禁用输入区的视觉层级，空房间增加“想说一点 / 先安静看看”双向引导，但不展示虚构人数、活动、消息或回应。桌面和移动预览 8 passed，默认生产隐藏与中英文框架 6 passed，类型、构建、差异格式和浏览器默认/展开/选中/禁用状态核对通过；生产包实测 `/peer-space` 为 404，成年人入口无解忧室文案。本批次仍无数据库、资格、消息 API、Realtime、发送或值班能力，不改变 READY WITH CONDITIONS。
- 2026-09-15 解忧室工作人员端首个界面预览：新增非生产 `/admin/peer-space` 工作台外壳，以能力上下文、示例班次、备班/安全占位、开放前核对和房间状态控制演示“先准备、再签到、后开房”的运营顺序；普通举报、安全事项和交接数量只显示明确说明的空状态，不读取学生数据。所有操作只改变当前页面，不授权、不保存、不通知，也未接入现有管理权限、排班、API、Realtime、审核队列或数据库。桌面/移动、既有学生端、双语、类型、构建、生产隐藏和浏览器视觉核对通过；中英文工作人员与学生路由在生产构建中均为 `notFoundRoutes`。本批次不代表工作人员已到岗、值班链路已配置或解忧室可以开放，不改变 READY WITH CONDITIONS。
- 2026-09-16 解忧室工作人员端任务优先简化：顶部改为阶段与当前任务摘要，开放前核对由五项归并为工作环境、人员与安全路径、规则与交接三组；签到后收起准备内容并直接定位房间控制，零待办与关键服务改为单一空状态和按需展开，开放、只读/交接、暂停分别形成主操作、次操作和二次确认的风险操作。操作反馈留在相关区域。本轮仍只使用本页临时状态，没有读取学生内容、接入真实账号、排班、API、Realtime、审核队列或数据库；生产路由继续强制隐藏。类型、生产构建、差异格式、学生端/工作人员端/双语回归、生产隐藏以及浏览器签到与暂停实操均通过，不改变 READY WITH CONDITIONS。
- 2026-09-16 解忧室学生端进入路径简化：受邀成年人入口从普通工具之后移到主视觉之后；首次告知由四张规则卡和独立隐私卡归并为实时与值班边界、身份与可见范围、隐私与安全三组，继续保留消息不自动到期、成员期、非咨询定位、隐私限制和人工安全复核边界。确认进入后焦点回到房间标题；空房间提示合并，输入区更早出现；身份设置只展示房间昵称、匿名和交流期待，经验证姓名与学校展示移入按需展开的暂未开放说明。所有选择仍只存在于当前页面，未接入真实资格、账号身份、消息、数据库、API、Realtime 或值班服务。类型、生产构建、差异格式、学生端/工作人员端/双语回归和浏览器完整流程核对通过；生产构建仍将中英文学生端与工作人员端路由列入 `notFoundRoutes`，成年人静态页面不含入口。本批次不改变 READY WITH CONDITIONS。

- 2026-09-17 解忧室授权基础：用官方 Supabase CLI 创建本地未应用 migration，并同步 `supabase/schema.sql`；建立成年人空间、cohort 邀请、规则确认、唯一公共房间和分段成员期。资格同时要求服务端当前 `18_plus`、`adult_self`、学生角色和 active 邀请；规则确认使用只授予 `service_role` 的原子函数，不读取客户端 user/school/cohort/room。两条授权 API 另由默认关闭的服务端开关保护；普通客户端业务表 deny-all，公共房间初始 `closed`；未创建消息、Realtime、值班能力、本校房间或 `14_17` 空间实例，生产入口仍强制关闭。专项 12 passed，生产默认隐藏 4 passed、12 skipped by design，类型与生产构建通过。由于本地没有可用 Supabase/Postgres 运行时，SQL 尚未执行验证；未连接正式项目，不改变 READY WITH CONDITIONS。

- 2026-09-17 解忧室工作人员本地基础：新增独立 capability + scope、不可恢复撤销与授权审计、当班/独立备班关系、五项签到确认、120 秒服务端租约、签到/心跳/交接/结束及过期回收函数，并同步 `supabase/schema.sql`；context/shifts API 默认关闭，普通客户端表权限 deny-all。该 migration 尚未应用，未接入真实人员、生产排班、持续回收调度、60 秒提醒、开房/只读/暂停、消息或 Realtime；本地专项测试的历史结果见 `ROADMAP.md`，不是数据库级执行证据，不改变 READY WITH CONDITIONS。
- 2026-09-19 工作树交付静态复核后，本地将同一房间限制为最多一条 active 和一条 handoff_pending 班次；旧班过期或主动结束仅在无另一有效班次时把开放房间降为只读，已同步 migration、schema、静态测试及设计草案。该约束仍待排班业务评审和独立测试数据库并发/交接、RLS、advisor 验证；不把 120 秒自动只读视为已验收。未连接正式 Supabase。
- 2026-09-19 当前工作树交付验证：`pnpm typecheck`、`pnpm build` 和 `git diff --check` 通过；构建成功生成 66 个静态页面，解忧室中英文学生端与工作人员端仍列入生产 `notFoundRoutes`。在清除远程测试凭据并禁用测试服务器启动的条件下，工作人员值班的 5 项非联网专项检查通过；依赖本地服务、真实数据库或远端凭据的 Playwright 测试未重跑。未运行本地 PostgreSQL 的 migration、RLS/advisor 或允许/拒绝查询；这些静态结果不能替代数据库级验收。
- 2026-09-19 后续本地并发修正：未应用的值班 migration 将签到、续租、交接与失效回收统一为先锁房间、再锁班次，回收在加锁后重检状态，避免旧班与新班竞争时的反向锁顺序。schema 与静态测试已同步；此后专项 Playwright 12 项、类型检查、构建和差异格式检查通过，生产预览路由仍在 `notFoundRoutes`。这些检查未执行数据库 SQL、RLS、advisors 或真实并发事务，不能作为自动只读验收证据。
- 2026-09-19 后续隔离数据库验证（补充上述静态检查，不改变其历史结果）：无网络、无挂载的临时 PostgreSQL 17 容器加载完整 consolidated schema；仅因原始镜像缺少平台 `auth.jwt()`，在容器内补了返回 `null` 的测试桩。合成数据验证交接重叠、第二条 active 唯一约束拒绝、旧班过期保留仍有新班覆盖的开放房间、最后一班结束转只读、有效续租及主班授权撤销后拒绝续租；确认值班表 RLS 开启、`anon`/`authenticated` 无表和启动 RPC grant，`authenticated` 直接查询被拒。两会话并发签到实测第二会话等待数据库锁，第一会话提交后被唯一约束拒绝。另以禁用远程测试凭据且不启动应用服务的方式重跑 28 项源代码/静态专项，全部通过；`pnpm typecheck`、`pnpm build`、中英文 1531 个翻译键结构对齐和 `git diff --check` 通过，生产构建仍将四条中英文解忧室预览路由列入 `notFoundRoutes`。未执行单独 migration 链、Supabase advisors、完整 RLS 允许/拒绝矩阵、交接/回收竞态或实际调度延迟验收；本项和 READY WITH CONDITIONS 均保持未关闭，未连接正式 Supabase。
- 2026-09-19 并行工作树随后新增 `20260919065043_add_peer_space_room_state_controls.sql`、房间状态 API 与专项测试，并将 schema 追加一张事件表及状态转换函数；上述 PostgreSQL 执行和 28 项静态回归发生在这些文件出现之前，不能覆盖当前最新 schema 或该 API。该批次须单独核对 migration/schema 一致性、开房主备班约束、暂停/恢复授权、审计及允许/拒绝路径后才可纳入交付提交；未应用至正式 Supabase，也未开放真实房间。
- 2026-09-19 最新工作树补充检查：房间状态批次 3 项非联网源代码/静态测试通过，前述 28 项亦在新增文件后重跑通过，合计当前快照 31 项；更新后的 `pnpm typecheck` 与 `pnpm build` 通过，构建包含默认关闭的房间状态 API。此结果仍不能替代新 migration 在 PostgreSQL 中执行、完整授权查询或真实开房/暂停操作验收。
- 2026-09-19 房间状态控制本地小阶段：新增未应用的第三条 migration、仅 `service_role` 可执行的原子房间状态函数和 deny-all 状态审计表；开房绑定当前 `room_duty` 当班、有效租约、独立备班、五项签到及成年人空间，安全暂停须由 `safety_duty` 先解除到只读。默认关闭的工作人员 API 不接入生产工作台或学生消息。成年人授权、值班、房间操作专项桌面/移动共 32 passed，`pnpm typecheck`、`pnpm build` 通过；本次 Docker daemon 不可用，新增 SQL 没有数据库级执行结果，不继承前一次隔离数据库的验证结论。持续回收调度、60 秒提醒、真实人员和完整运营验收仍开放；未连接正式 Supabase，READY WITH CONDITIONS 不变。

- 2026-09-20 成年人账户工作台改为仅在服务端现有解忧室资格接口返回允许时显示进入卡片；本地虚构成年会话的未受邀、接口关闭和受邀状态在桌面、手机各 3 passed，类型与构建通过。公开说明页不是资格凭据。自行申报的 `18_plus` 不等于大学生身份核验；注册后申请与管理员预先邀请的双入口仍是待确认的产品、数据库和授权工作，未创建审核队列、生产账号或新权限，READY WITH CONDITIONS 不变。

## 未能自动证明的事项

- QQ、163、Outlook 当次外部邮件投递，自有 SMTP 与 DNS 信誉。
- 试点值班提醒的香港 SMTP 配置、真实收件/失败重试，以及值班时段、替补人与线下路径。
- 真实 iPhone/Android/微信内置浏览器、真实微信小程序 AppID 与审核。
- 外部告警接收端，以及 Lighthouse、Nginx 和应用容器的受控故障告警闭环。
- 五个支持入口（含已关闭 Talk 的兼容 API）的危机固定文案、词表误报/漏报样例和校内线下承接流程仍需心理专业、安全/隐私及学校负责人共同审核签字；自动化通过不能替代该审核。
- 校内心理老师、专业机构、机构人员、学校合作、转介授权、访问到期与撤销仍未实现或验证；需要产品、学校、隐私/安全和专业服务方共同确认最小共享范围与操作责任。
- 正式数据的隔离数据库恢复、数据库 RTO 实测、恢复关系上的 JWT/RLS 验收及删除重放已于 2026-08-11 完成；每日自动加密备份已连续成功两次，第二个包也已完成 Mac 异地同步和校验。真实邮箱 OTP/session 的隔离应用验收、跨日连续周期观察、外部失败告警和完整业务 RPO/RTO 仍待完成。
- 正式域名与香港 Lighthouse 已上线；Vercel 账户集成断开及适用备案/合规事项仍属于平台或账户侧人工操作。

只有 `ROADMAP.md` 的 PILOT BLOCKERS 全部关闭后，才可将结论提升为 **READY**。
