# 本地浏览器核对记录（2026-09-19）

检查环境：本地 Next.js 开发服务器；Playwright desktop-chromium 与 mobile-chromium；无隔离 Supabase 测试凭据。无真实用户数据或私人截图。

| 范围 | 实际核对 | 结果 |
|---|---|---|
| 中文和英文公开页面 | `/`、`/en/`、语言 cookie、`html lang`、家长公开页、青少年页、成年人页、SWEET 与工具页 | 自动化通过 |
| SWEET 花园 | `/garden` 与 `/en/garden` 访客入口、移动宽度、私有 API 访客 401、成长规则单元断言 | 自动化通过；登录态读写未验证 |
| 成年人聊天室 | `/peer-space` 与 `/en/peer-space` 访客页；房间、消息与规则 API 访客拒绝；移动无横向溢出 | 自动化通过；受邀账号聊天未验证 |
| 工作人员审核 | `/admin/peer-space` 与英文移动页访客不见队列，API 返回 401 | 自动化通过；有权审核未验证 |
| 咨询 | `/consultation` 公开中英文“暂未开放”与访客 API 拒绝；移动宽度 | 自动化通过；内部许可账号流程未验证 |
| 双语键 | `zh-CN` 与 `en` 均为 1899 个键、结构差异 0 | 静态核对通过 |
| 账户年龄工作台、人员申请、轻量反馈 | 组件/路由构建与访客拦截 | 构建通过；登录态交互未验证 |

`pnpm test:e2e`：477 passed、47 skipped、0 failed（共 524），其中涉及真实登录态或隔离项目数据的用例因缺少隔离凭据而跳过。`pnpm typecheck`、`pnpm build` 通过。RLS 与 Migration 尚未在隔离数据库执行；真机、微信内置浏览器、邮件送达和学校运营演练也未完成。对公开页面没有观察到测试报告中的横向溢出、控制台错误、React key 或 hydration 错误；这不覆盖已登录页面。

最后一次标题、索引与路由集成修改后，桌面和移动的 60 条定向用例全部通过；最终 `pnpm typecheck` 和 `pnpm build` 也再次通过。
