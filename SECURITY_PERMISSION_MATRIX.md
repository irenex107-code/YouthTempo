# Security and Permission Matrix

| 能力/数据 | 未登录 | 学生/青年 | 监护人 | 支持老师 | 学校负责人 | 平台管理员 | 专业支持者 |
|---|---|---|---|---|---|---|---|
| 公开内容 | 读 | 读 | 读 | 读 | 读 | 读 | 读 |
| 本人 SWEET/工具记录 | 禁止 | 本人读写删（需当前有效同意；学生自主试用可使用本人确认） | 无学生数据访问 | 禁止写 | 禁止写 | 运维边界内 | 本人范围 |
| 学生 SWEET 记录 | 禁止 | 本人 | 禁止，包括历史 active 关系 | 仅已分配学生 | 仅本校学生 | 全平台管理范围 | 无默认访问 |
| 留言 | 禁止 | 本人发送/查看授权消息；可主动发给已启用的试点值班负责人 | 无学生消息访问 | 分配范围 | 本校运营范围 | 仅 active 平台管理员可处理试点值班/安全队列并留下审计 | 验证后授权范围 |
| 社区 | 公开状态内容 | 按角色/学校可见，需同意 | 按角色可见 | 按角色/学校可见 | 本校管理可见 | 全平台审核 | 验证角色可见 |
| 举报/审核 | 禁止 | 举报本人可见内容 | 同左 | 同左 | 同左 | 审核、恢复、限制、审计 | 同普通验证用户 |
| 学校成员/关系 | 禁止 | 禁止 | 禁止 | 仅查看职责范围 | 本校分配/撤销 | 跨校平台管理 | 禁止 |
| 角色/学校字段 | 禁止 | 禁止客户端修改 | 禁止 | 禁止 | 仅可信工作流 | 可信服务端工作流 | 禁止 |
| 专业认证 | 禁止 | 可申请 | 可申请 | 可申请 | 可申请 | 审核/撤销 | 本人状态 |
| AI 辅助记录小结 | 仅已登录且完成适用有效同意的青少年/青年本人可生成；敏感保存继续受身份/同意限制 | 本人当次最小化输入；不得用于诊断、治疗、风险判断或支持决策 | 无权代替学生生成或查看未授权输入 | 无权代替学生生成或查看未授权输入 | 无权代替学生生成或查看未授权输入 | 仅脱敏运维边界 | 无默认访问 |
| 导出/注销 | 禁止 | 本人 | 本人 | 本人 | 本人 | 本人及合规运维 | 本人 |

上表描述本功能分支拟实施的边界；正式站在 Migration 与代码部署获批前仍按旧版本运行。历史段落描述旧决策时以 `ROADMAP.md` 的 2026-09-19 方向调整为准。其中“支持老师”将在产品文案中明确为班主任、导师等**学生支持老师**；当前“专业支持者”只有个人资质核验和社区身份能力，不能解释为校内心理老师、平台专业志愿者或具体学生服务关系。

## 已确认但尚未实现的专业支持目标边界

本节是 `PROFESSIONAL_SUPPORT_MODEL_DECISION.md` 确认的目标模型，不是当前生产权限。完成数据库、服务端、界面和允许/拒绝测试前，不得据此授予访问。

| 能力/数据 | 学生支持老师 | 校内心理老师 | 平台专业志愿者 |
|---|---|---|---|
| 工作归属 | 学校成员；班主任、导师或年级负责老师 | 学校成员；由学校登记岗位 | 由 YouthTempo 直接招募；机构经历或合作关系可选，不是普通学校成员 |
| 专业资质 | 不因岗位自动获得专业身份 | 承接专业支持前须由平台确认个人资质 | 承接服务前须由平台确认个人资质及获准服务范围 |
| 学生范围 | 仅学校明确分配的学生 | 仅明确转介或分配且授权有效的学生 | 仅本人已接受且授权有效的具体专业支持事项 |
| SWEET 数据 | 当前政策下仅分配学生的记录 | 仅转介所需且经授权共享的最少内容 | 默认不可读完整历史；仅查看转介包明确共享的最少内容 |
| 悄悄话信箱 | 仅学生直接发送给本人或职责范围内的授权消息 | 仅学生直接发送给本人；不因此创建专业支持关系 | 不接收；该信箱不是专业支持入口 |
| 专业支持沟通 | 默认无 | 仅已接受且授权有效的事项 | 仅本人已接受且授权有效的事项 |
| 协作记录 | 可记录非诊断性的学校跟进状态 | 可记录接收、联系、下一步、转外部和结束状态 | 可记录接收、联系、下一步和结束状态 |
| 完整咨询、诊断或治疗病历 | 禁止 | 不在 YouthTempo 保存 | 不在 YouthTempo 保存 |
| 发起或处理转介 | 可提出转介请求 | 可接受校内转介并建议其他支持 | 可接受或退回已授权的专业支持事项 |
| 平台试点值班/安全队列 | 无默认访问 | 无默认访问；须另行授予值班职责 | 无默认访问；须另行授予值班职责 |
| 人员与学校管理 | 禁止 | 禁止 | 禁止 |

目标模型按三类独立事实授权：学校岗位或平台招募关系决定“以什么身份参与”，个人专业核验决定“能提供哪类支持”，有效的学生分配或专业支持事项决定“能看谁的哪些数据”。同一账号可以同时持有多项事实，不能再由一个 `profiles.role` 值替代。机构关系仅为未来可选事实。

## 已确认、授权基础已本地实现的成年人解忧室目标边界

本节是 `PEER_SPACE_PRODUCT_DECISION.md` 与 `PEER_SPACE_ADULT_MVP_DESIGN_DRAFT.md` 确认的产品边界，不是当前生产权限。截至 2026-09-19，本地已有成年人邀请、规则确认、唯一公共房间成员期、工作人员独立 capability + scope、值班租约，以及原子开房/只读/暂停控制和审计；三条 migration 均尚未应用，相关 API 服务端开关默认关闭，房间初始为 `closed`，生产入口继续关闭。学生端交互见 `PEER_SPACE_STUDENT_UX_DESIGN_DRAFT.md`，值班/审核/安全/配置工作台见 `PEER_SPACE_STAFF_UX_DESIGN_DRAFT.md`，数据库、API、RLS 和审核能力见 `PEER_SPACE_TECHNICAL_DESIGN_DRAFT.md`；消息、Realtime、审核/安全队列、持续租约回收调度、60 秒提醒、正式迁移和数据库级允许/拒绝验收完成前不得开放入口或内容访问。

当前值班 migration 本地暂按交接流程约束每房间最多一条 `active` 和一条 `handoff_pending`；旧班结束或过期时，只有没有另一条租约、时段及主备班授权均有效的班次才降为只读。2026-09-19 临时隔离 PostgreSQL 的合成数据已验证交接重叠、第二条 active 的并发拒绝、过期与主动结束的房间降级、撤权后续租拒绝，以及普通角色的表/RPC grant 拒绝；这不是正式项目或完整允许/拒绝矩阵验收。撤权会立即使授权不再计入有效覆盖，但现有授权撤销触发器本身不切换房间状态；仍须决定并验证撤权后的房间状态与内容鉴权收敛、交接/回收竞态、完整 RLS 路径、advisors 和实际调度，不把自动只读声明为已验收能力。

| 能力/数据 | 受邀成年学生 | 未受邀成年人/14–17 岁学生 | 家长/老师/学校/专业身份 | 房间值班/内容审核员 | 安全值班负责人 | 平台配置管理员 |
|---|---|---|---|---|---|---|
| 解忧室入口 | 符合 `18_plus`、`adult_self` 和试点邀请时可见；接受规则后自动建立大家房间 membership | 完全隐藏 | 不因角色可见 | 仅工作入口 | 仅工作入口 | 仅配置入口 |
| 大家的解忧室 | 不同获批 cohort、不同学校的 active 成年成员可见；规则确认后自动加入，不设人数门槛 | 禁止 | 禁止 | 仅获派工作范围 | 仅安全事件所需 | 默认禁止 |
| 本校解忧室 | 首期禁止；不创建 room、membership、topic 或入口，未来升级须重新批准 | 禁止 | 学校关系不创建聊天室或内容访问 | 禁止 | 禁止 | 禁止配置 |
| 实时历史与接收 | 仅本人获准房间且仅有效成员期内产生的消息；正文每次经 API 鉴权获取 | 禁止 | 禁止 | 仅获派范围 | 仅安全事件所需 | 默认禁止 |
| 发送消息 | 房间 `staffed_open` 且本人未禁言时允许；先检查后持久化和通知 | 禁止 | 禁止 | 普通工作人员不能伪装学生；官方通知另行授权 | 仅官方安全提示另行授权 | 禁止 |
| 对外身份 | 公共房间选择匿名、房间昵称或经验证姓名；默认房间昵称，模式变化动态作用于当前及既往消息；实名须 active 学校名单确认 | 禁止 | 学校可在名单管理职责内确认姓名，但不能代选或得知使用情况；其他角色禁止 | 只看同伴可见身份 | 仅 active 安全事件可查内部映射 | 只看功能就绪状态，不能直接填写姓名或代选 |
| 经验证姓名记录 | 仅本人在身份设置中查看自己的当前确认姓名 | 禁止 | 学校负责人仅可确认、更正或撤销本校名单姓名；无解忧室活动数据 | 禁止 | 不因安全职责获得名单管理权 | 平台受信任名单管理流程；与解忧室配置能力分离 |
| 展示学校 | 仅大家房间、仅本人、默认隐藏；只显示当前验证学校 | 禁止 | 不能代学生开启，也不因此获得内容 | 禁止代操作 | 禁止代操作 | 禁止代操作 |
| 本人删除/退出 | 可随时删除；退出立即失权但不自动删除消息，重新加入不获得离开期间历史 | 禁止 | 禁止 | 禁止代操作 | 禁止代操作 | 禁止代操作 |
| 举报与屏蔽 | 仅本人可见消息和同一年龄空间成员 | 禁止 | 禁止 | 处理获派举报 | 仅处理安全事件 | 默认禁止 |
| 账号—房间身份映射 | 仅本人 | 禁止 | 禁止 | 禁止 | 仅 active 安全事件、原则上双人批准并审计；紧急例外 24 小时内补审 | 默认禁止 |
| 试点邀请、房间与排班 | 禁止 | 禁止 | 学校只参与可信学校归属和运营联系人确认，不审批学生加入或读取房间 | 仅当前获派班次和工作范围 | 禁止配置，除非另获能力 | 可管理自动建房和排班，但不因此读取正文 |
| 学校运营信息 | 只看本人状态 | 禁止 | 仅房间已配置、值班准备完成等纯运营状态；无学生人数、活跃度、消息、主题、举报或其他使用统计 | 仅本人工作状态 | 仅本人安全职责状态 | 仅配置和人员准备状态 |

解忧室使用独立 `peer_space_*` 权限域；不直接复用现有社区的帖子/评论、`viewer_roles` / `commenter_roles`、真实姓名/邮箱回退或平台管理员全量读取模型。`cohort` 只管理邀请，`room` 决定内容范围。普通客户端对业务表保持 deny-all，由 Pages Router API 重复验证 bearer token、服务端年龄/同意、space、room、membership、学校范围和工作人员 capability。私有 Realtime 频道只传随机消息 ID 等最小事件，聊天正文仍经 API 逐次鉴权；客户端没有直接 Broadcast 发送权限。

## 强制边界

1. API 先用 Supabase `getUser(token)` 认证，再查数据库关系；不信任客户端角色、school ID 或验证状态。
2. RLS、列级 grant、触发器与受限 RPC 是纵深防护，不替代 API 授权。
3. `service_role` 只存在服务端；浏览器和小程序只能使用 publishable/anon key。
4. 学生同意撤回后不能新增 SWEET、留言或社区内容，但仍可依法查看、导出或删除已有本人数据。
5. 自动化权限 fixture 只能写入独立 E2E Supabase 项目；正式项目与恢复演练项目由脚本和 Playwright 配置双重拒绝，测试结束必须撤销 session 并清理固定账号、关系和邮箱引用。
6. `safety_review` 内容不可被客户端绕过、静默发布或自行改变审核状态。
7. Check-in、Mood Journal、Talk、Worry Time 与 Referral 的用户文本先执行共享中英文确定性危机识别；命中后不得调用 AI provider 或继续普通反思，并只返回固定现实支持指引。Talk 的普通生成在首轮学校试点期间关闭，保留接口只为兼容旧客户端并优先返回固定危机指引。危机状态只由确定性规则和真人流程承接，模型输出不得参与风险判断。
8. 只有 Check-in 与 Mood Journal 的普通路径调用 AI provider，且模型只能从最小化来源字段中选择字段 ID；服务端拒绝未知、重复、超量或带额外输出字段的结果，并用固定模板组合最终小结。普通请求必须通过服务端登录、有效同意、当前 `AI_NOTICE_VERSION`、AI 总开关、provider 主机白名单及日期模型快照白名单校验。客户端勾选不能替代服务端校验。结果须持续显示 AI 身份标识，固定安全提示不得误标为 AI 生成。
9. Worry Time 与 Referral 的普通路径分别使用版本化确定性规则，不调用 AI provider；自由文字只能参与共享危机检查或用户本人选择的规则化整理，不能用于评分、诊断、风险判断或自动通知学校。结构化选项只能决定整理起点，不能替代真人评估。
10. 学生自主试用仅接收 14 岁及以上用户。14–17 岁的 `student_self_pilot` 依据只放行学生本人功能，不代表监护人确认，不创建学校、老师或监护人访问权；正式学校试点必须启用新的同意版本并重新审核适用确认。
11. 学生主动发给试点值班负责人的留言，以及无学校承接的 `safety_review` 留言，可以由 active 平台管理员在值班后台查看。普通私人记录不进入队列。提醒邮件只能含事件类型、随机编号、时间和后台链接，不能包含学生身份或正文；状态处理必须保留审计记录。
12. 悄悄话信箱用于学生向已关联老师、家长或自己留言，不是专业支持入口；除既有安全升级外，留言不得自动创建专业支持事项或向专业人员开放。
13. 18 岁及以上用户不建立新的监护访问关系；当前试点不要求任何学生加入家长。应用界面、名单导入、账户/消息 API 和关系创建 API 均默认关闭家长流程，并保留解除旧关系能力。未满 18 岁的家长可选加入功能在后续政策和权限设计批准前保持关闭；旧 active 成人关系的 RLS 年龄/同意约束仍须单独批准迁移并完成直接客户端回归。
14. 在专业支持目标模型实现前，校内心理老师、平台专业志愿者或外部机构人员没有新增的学生数据访问权；现有专业身份确认只证明平台核验了相应资料，不自动创建学校、机构、学生或值班关系。
15. 解忧室真实服务尚未实现或开放。2026-09-17 已本地实现两批授权基础：只允许 `18_plus` + `adult_self` + 受邀学生确认规则后建立唯一跨校大家房间成员期；工作人员只按独立、当前有效且具 space/room scope 的 `room_duty`、`content_moderator`、`safety_duty`、`config_admin` capability 进入最小工作上下文，不能从其他身份推断。两条 migration 均尚未应用，API 默认关闭，公共房间保持 `closed`。主动退出后不得静默自动重加。首期不创建本校房间、成员关系、topic、入口或候补；学校验证仅支持本人自愿展示学校及经验证姓名。不同 cohort 可共同进入大家房间，`14_17` 仍无空间实例、入口或 API。家长、老师、学校、专业身份和普通平台管理员没有默认内容访问权；尚未实现的内容审核、安全值班、配置操作和身份映射查看不能因 capability 表已存在而视为已授权。
16. 公共房间允许学生选择匿名、房间昵称或经验证姓名；默认房间昵称，不能逐条消息切换，变更应动态作用于平台内当前及既往消息。匿名只对同伴隐藏，平台保留安全映射。首批经验证姓名只来自带确认人、时间、学校、来源和撤销/更正审计的 active 学校名单确认记录；不收身份证件，不得回退到可编辑 `profiles.display_name`、邮箱、Auth metadata、自由输入或现有邀请姓名。姓名确认工作流未完成前实名选项全局关闭。
17. 姓名确认记录失效、进入更正、对应学校验证失效或姓名内容变化时，公共房间须立即回退为房间昵称，当前及历史消息同步更新。重新确认后不得自动恢复实名，必须由学生重新选择；学校不得获知学生是否选择实名或任何房间活动。
18. 解忧室消息不设自动到期，但学生历史按每一段有效公共房间 membership 隔离；首段从规则确认入房起算，新成员不能读取加入前内容，重新加入不能读取离开期间内容。每个开放房间至少有 1 名当班和 1 名独立备班/安全联系人；30 秒续租、60 秒提醒、120 秒自动只读，失权 API 立即拒绝且旧 Realtime 连接最多 5 分钟完全停止。
19. 解忧室 1 小时/24 小时禁言可由获准审核员单独执行；7 天禁言或移出须第二人批准并提供 7 天申诉期。学校只获得房间配置、值班准备等纯运营状态，不获得学生人数、活跃度、消息、主题、举报或其他使用统计。
