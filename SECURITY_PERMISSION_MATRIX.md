# Security and Permission Matrix

| 能力/数据 | 未登录 | 学生/青年 | 监护人 | 支持老师 | 学校负责人 | 平台管理员 | 专业支持者 |
|---|---|---|---|---|---|---|---|
| 公开内容 | 读 | 读 | 读 | 读 | 读 | 读 | 读 |
| 本人 SWEET/工具记录 | 禁止 | 本人读写删（需同意） | 禁止写 | 禁止写 | 禁止写 | 运维边界内 | 本人范围 |
| 学生 SWEET 记录 | 禁止 | 本人 | active 关系孩子完整记录 | 仅已分配学生 | 仅本校学生 | 全平台管理范围 | 无默认访问 |
| 留言 | 禁止 | 本人发送/查看授权消息 | 关系范围 | 分配范围 | 本校运营范围 | 安全运营范围 | 验证后授权范围 |
| 社区 | 公开状态内容 | 按角色/学校可见，需同意 | 按角色可见 | 按角色/学校可见 | 本校管理可见 | 全平台审核 | 验证角色可见 |
| 举报/审核 | 禁止 | 举报本人可见内容 | 同左 | 同左 | 同左 | 审核、恢复、限制、审计 | 同普通验证用户 |
| 学校成员/关系 | 禁止 | 禁止 | 禁止 | 仅查看职责范围 | 本校分配/撤销 | 跨校平台管理 | 禁止 |
| 角色/学校字段 | 禁止 | 禁止客户端修改 | 禁止 | 禁止 | 仅可信工作流 | 可信服务端工作流 | 禁止 |
| 专业认证 | 禁止 | 可申请 | 可申请 | 可申请 | 可申请 | 审核/撤销 | 本人状态 |
| AI 辅助记录小结 | 仅已登录且完成适用有效同意的青少年/青年本人可生成；敏感保存继续受身份/同意限制 | 本人当次最小化输入；不得用于诊断、治疗、风险判断或支持决策 | 无权代替学生生成或查看未授权输入 | 无权代替学生生成或查看未授权输入 | 无权代替学生生成或查看未授权输入 | 仅脱敏运维边界 | 无默认访问 |
| 导出/注销 | 禁止 | 本人 | 本人 | 本人 | 本人 | 本人及合规运维 | 本人 |

## 强制边界

1. API 先用 Supabase `getUser(token)` 认证，再查数据库关系；不信任客户端角色、school ID 或验证状态。
2. RLS、列级 grant、触发器与受限 RPC 是纵深防护，不替代 API 授权。
3. `service_role` 只存在服务端；浏览器和小程序只能使用 publishable/anon key。
4. 学生同意撤回后不能新增 SWEET、留言或社区内容，但仍可依法查看、导出或删除已有本人数据。
5. `safety_review` 内容不可被客户端绕过、静默发布或自行改变审核状态。
6. Check-in、Mood Journal、Talk、Worry Time 与 Referral 的用户文本先执行共享中英文确定性危机识别；命中后不得调用 AI provider 或继续普通反思，并只返回固定现实支持指引。Talk 的普通生成在首轮学校试点期间关闭，保留接口只为兼容旧客户端并优先返回固定危机指引。危机状态只由确定性规则和真人流程承接，模型输出不得参与风险判断。
7. 只有 Check-in 与 Mood Journal 的普通路径调用 AI provider，且模型只能从最小化来源字段中选择字段 ID；服务端拒绝未知、重复、超量或带额外输出字段的结果，并用固定模板组合最终小结。普通请求必须通过服务端登录、有效同意、当前 `AI_NOTICE_VERSION`、AI 总开关、provider 主机白名单及日期模型快照白名单校验。客户端勾选不能替代服务端校验。结果须持续显示 AI 身份标识，固定安全提示不得误标为 AI 生成。
8. Worry Time 与 Referral 的普通路径分别使用版本化确定性规则，不调用 AI provider；自由文字只能参与共享危机检查或用户本人选择的规则化整理，不能用于评分、诊断、风险判断或自动通知学校。结构化选项只能决定整理起点，不能替代真人评估。
