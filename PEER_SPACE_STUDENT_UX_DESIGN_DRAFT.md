# 成年人解忧室学生端页面与交互设计草案

> **2026-09-19 新方向提示：** 本文保留早期决策/设计脉络；与 `ROADMAP.md` 会前版本方向冲突的唯一公共房间、可展示经验证姓名、无普通倾听志愿者或尚未实现的描述均以新路线图和 `docs/TEACHER_REVIEW_READINESS.md` 为准。代码仍处开发分支，正式环境未开放。

状态：**2026-09-13 学生端 UX 草案；2026-09-17 已有默认隐藏、生产强制关闭的非生产页面预览，以及尚未应用的本地成年人准入基础。预览未接入真实资格、消息或 Realtime；本文件不批准后续功能、正式数据库迁移、生产开放或 14–17 岁入口。**

关联基线：

- `PEER_SPACE_PRODUCT_DECISION.md`
- `PEER_SPACE_ADULT_MVP_DESIGN_DRAFT.md`
- `PEER_SPACE_TECHNICAL_DESIGN_DRAFT.md`

## 1. 设计目标

让受邀成年学生在手机或桌面端都能清楚完成以下路径：

1. 从现有 18–25 岁页面找到解忧室，同时不让未受邀成年人或中学生看到未开放入口。
2. 第一次进入时理解实时、房间范围、学校展示、值班和安全边界。
3. 完成规则确认后自动成为“大家的解忧室”成员并进入公共聊天室；首期不显示本校聊天室。
4. 在公共房间选择匿名、房间昵称或经验证姓名，并自行决定是否展示当前验证学校。
5. 清楚区分消息发送中、已发送、失败、断线、只读和暂停状态。
6. 不离开主要聊天路径即可调整交流期待、删除、屏蔽、举报或寻找现实支持。

解忧室界面不能看起来像专业问诊、客服工单、交友应用或家校医社区，也不能暗示“发出后一定马上有人回应”。

## 2. 与现有界面的关系

### 保留不动

- 顶部身份入口结构保持不变。
- `14_17` 中学生页面不增加解忧室卡片、灰色入口或“暂未开放”。
- 家长页面不增加解忧室内容或入口。
- “家校医社区”继续使用现有帖子/评论页面。
- “悄悄话信箱”继续是学生定向写给老师、未来适用的家长、试点值班或自己的页面。

### 沿用的视觉语言

- 沿用现有 `cream`、`paper`、`mist`、`sage` 色系、圆角卡片、圆形主按钮和低对比阴影。
- 入口和首次告知使用现有 `PageHero`、`SectionHeader`、`card`、`button-primary` / `button-secondary` 语言。
- 聊天室为持续操作界面，不使用占据大量首屏的 PageHero；改用紧凑顶栏、滚动消息区和固定输入区。
- 不复用信箱插图或社区帖子视觉，避免误导功能关系。后续如需要入口插图，应制作独立的“被倾听、围坐交流”主题素材。

## 3. 信息架构与路由

| 路径 | 页面职责 | 普通学生是否可发现 |
|---|---|---|
| `/for-young-adults` | 现有成年人功能入口；符合解忧室资格时增加动态卡片 | 仅受邀且年龄/同意有效时显示解忧室卡片 |
| `/peer-space` | 根据服务端状态显示登录续接、首次告知，并在激活后进入公共聊天室 | 直接访问仍由服务端判断，不泄露房间或成员信息 |
| `/peer-space/rooms/[id]` | 实时聊天室 | 仅 active room membership；不可用时返回通用状态 |
| `/peer-space/reports` | 查看本人举报处理状态 | 仅本人；不显示审核人员或他人处置细节 |

首批不在全站 Navbar 增加固定“解忧室”项目。原因不是弱化功能，而是入口必须随年龄、同意、邀请和房间开放状态变化，不能让未受邀用户形成可申请或已经开放的预期。

## 4. 入口卡片

### 显示条件

`/for-young-adults` 加载后调用最小 access 状态：

- `eligible + invited/active`：显示解忧室卡片。
- 未登录、未受邀、年龄/同意不符或 `14_17`：不显示卡片，不留空位。
- access 请求失败：不显示卡片，不把网络错误变成资格提示；页面其他成年人功能正常使用。

### 建议文案

- 标题：**解忧室**
- 状态标签：**值班开放** / **暂时只读** / **当前未开放**
- 说明：**有人值守时，和其他成年学生实时聊聊。你可以选择匿名、昵称或经验证姓名；学校默认隐藏。**
- 主操作：**进入解忧室**
- `read_only` 操作：**进去看看**

不使用“在线咨询”“匿名交友”“找搭子”“专业老师陪聊”或“随时有人回应”。

## 5. `/peer-space` 页面状态机

同一路径承载五种状态，避免建立多个容易绕过的页面：

### 5.1 加载中

- 显示与公共房间结构一致的 skeleton，不先闪现房间名称或资格文案。
- 页面标题保持“解忧室”，不显示学校或成员信息。

### 5.2 Session 已过期

- 标题：**登录后继续**
- 说明：**登录后，我们会重新确认你是否可以进入。**
- 操作：**登录账号**，携带安全的 `next=/peer-space`。
- 不在登录前确认该邮箱是否受邀。

### 5.3 当前不可用

- 使用统一、不枚举原因的提示：**这个入口目前不适用于你的账号。你仍然可以使用个人记录和支持工具。**
- 操作：**返回我的入口**、**查看支持路径**。
- 不显示“你未满 18 岁”“你的学校未加入”“邀请已撤销”等可用于枚举内部关系的细节。

### 5.4 尚未接受规则

- 显示首次告知页；完成确认后，服务端自动创建“大家的解忧室”成员关系并进入公共聊天室，不需要申请或等待人数门槛。
- 若规则版本更新，再次显示差异摘要并要求确认；不能静默替用户接受。

### 5.5 已激活

- 显示公共聊天室或其只读/暂停状态。

## 6. 首次告知页

告知应在一个移动端页面内完成，不用长篇协议或多步轮播阻挡理解。

### 页面结构

1. 紧凑标题：**进入解忧室前，先说清几件事**。
2. 四张简短说明卡：
   - **这是实时群聊**：消息安全通过后会发送给同一房间里的成年学生。消息不自动到期，但你只能看到自己加入房间以后、且仍在有效成员期内产生的内容。
- **你决定别人怎样看到你**：公共聊天室可选择匿名、房间昵称或经验证姓名；学校默认不显示，是否展示由你决定。匿名只对其他学生隐藏，不对平台安全流程隐藏。
   - **有人值守，但不是咨询**：值班人员维护秩序与安全，不保证马上回复，也不替代专业或紧急支持。
   - **需要时会进行安全复核**：普通内容不会自动交给家长、老师、学校或专业人员；明确危险内容可能不公开并进入人工安全流程。
3. 一张“请不要发布”的提醒：真实姓名、院系/班级、住址、电话、社交账号、他人可识别信息。
4. 一条始终可访问的支持资源链接。
5. 复选框：**我理解这些边界，并愿意遵守解忧室规则。**
6. 主操作：**同意并进入**；次操作：**暂时不进入**。

确认按钮只有在复选框勾选后可用。返回不等于拒绝 YouthTempo 其他功能。

## 7. 公共聊天室入口

### 页面顶部

- 标题“解忧室”。
- 一句短说明：**这里是跨学校的公共聊天室，你可以说说，也可以先安静看看。**
- 当前整体状态：值班开放、暂时只读或暂停。
- “规则与隐私”入口，不在首屏重复整页告知。

### 7.1 大家的解忧室

- 固定为首张主卡。
- 完成首次规则确认后自动加入；主动退出后不再被后台静默加回，只有本人明确重新加入才创建新的成员期。
- 范围说明：**来自不同学校的成年学生都可能在这里。**
- 身份摘要示例：**当前身份：匿名同学 · 学校已隐藏**、**当前身份：云边纸船 · 学校已隐藏**，或 **当前身份：经验证姓名 · 学校显示为浙江大学**。
- 状态与操作：**值班开放 · 进入聊天**；只读时为 **暂时只读 · 查看消息**。
- 不显示精确在线人数、消息总数、学校数量、热度或“最活跃房间”。

### 7.2 不显示未来房间

- 本校聊天室和主题聊天室首期均不显示卡片、灰色入口、“暂未开放”、申请或候补。
- 学校认证只可能使公共聊天室的学校展示和经验证姓名可用，不新增聊天室。
- 未来升级统一记录在 `PEER_SPACE_FUTURE_UPGRADES.md`，不能提前在当前页面占位。

### 房间菜单

- **需要更具体的支持？** 跳转独立支持资源/专业支持入口，不携带聊天室正文。
- **我的解忧室设置**：规则版本、屏蔽名单、本人举报、退出解忧室。

## 8. 聊天室布局

### 8.1 桌面端

- 内容区最大宽度约 760–840 px，保持聊天阅读密度，不铺满 76rem 容器。
- 顶部为粘性房间栏：返回成年人入口、房间名、跨学校范围说明、值班状态和更多菜单。
- 中间为独立滚动消息区。
- 底部为粘性输入区；桌面端仍保证键盘焦点和页面缩放下可见。
- 可选窄侧栏只放规则、支持资源和本人身份设置，不放成员列表。

### 8.2 移动端

- 聊天室使用接近全高布局，但保留现有 Navbar 和语言切换。
- 房间栏、消息区、输入区三段明确；输入区适配 iOS/Android safe area 和软键盘。
- 打开举报、屏蔽、身份设置时使用底部 sheet 或全屏 dialog；关闭后焦点回到触发按钮。
- 不依赖 hover；消息操作通过明确的“更多”按钮，而不是只能长按。

### 8.3 消息样式

- 他人消息：白色或 `paper` 卡片，显示该成员当前选择的匿名标识、房间昵称或经验证姓名，以及可选学校标签、当前交流期待、正文和分钟级时间。
- 本人消息：使用淡 `mist` 背景和“你”标识，同时显示本人在该房间的当前对外身份，帮助理解他人看到的样子。
- 官方值班消息：使用 `cream` 提示卡和清晰“解忧室值班”标识，不伪装成学生。
- 已删除消息：在需要保持上下文时显示中性占位“这条消息已删除”，不保留作者学校标签或正文长度。
- 审核中消息只对作者显示“暂未公开，正在进行安全复核”；其他学生不看到占位。

聊天消息不使用点赞、热度、连续发言徽章或公开发言次数。

## 9. 滚动与新消息

- 初次进入定位到最新的已授权消息，先加载 50 条，向上每次再加载 50 条。
- 历史只覆盖本人每一段有效公共房间成员期，首段从规则确认并自动入房时开始；后来加入者看不到加入前的消息，退出或被移出后重新加入也看不到离开期间产生的消息。
- 断网、关闭页面或退出登录不等于退出房间；只要成员资格仍有效，返回后仍可查看该段成员期内的消息。
- 用户停留在底部时，新消息平滑追加；尊重 reduced-motion 设置。
- 用户正在阅读旧消息时不强制跳到底部，显示 **有新消息** 浮动按钮。
- 切换房间前保存当前已读游标，但不记录精确“谁读了哪条”供其他学生查看。
- 屏蔽、删除或审核状态变化后，当前视图和本地缓存同步移除相应内容。
- 房间长时间无学生发言时如实显示：**现在比较安静。你可以先说一句，也可以晚点再来。** 不伪造在线人数或自动生成同伴回应。

## 10. 房间身份设置

点击顶栏中的本人身份，打开“我在这个房间的身份”。公共房间默认使用房间昵称；不允许逐条消息切换身份，避免同一段对话出现难以理解的混合身份。

### 三种对外身份

- **匿名**：其他学生看到稳定的房间内匿名标识，例如“匿名同学·7K3”；平台仍保留用于安全、举报和审计的内部账号关系。
- **房间昵称**：显示系统生成且可有限更换的昵称，例如“云边纸船”。
- **经验证姓名**：只在学校已通过受信任名单流程确认姓名时可选，并明确提示会向同房间学生公开准确姓名。当前可编辑的 `profiles.display_name`、邮箱前缀、自由输入或尚未生成独立确认记录的邀请姓名不能作为经验证姓名来源。

切换后，平台内本人在该房间的当前及既往消息统一按最新身份模式展示，避免旧消息永久暴露已经关闭的身份。界面必须同时提示：已经被他人看过、复制或截图的信息无法由平台收回。

姓名确认功能尚未完成或全局关闭时，不展示一个看似可用却永远无法完成的实名操作；功能获批后，没有 active 学校姓名确认的用户看到禁用状态和说明：**当前没有学校确认的姓名，你仍可使用匿名或房间昵称。** 不引导上传身份证件。

若姓名确认被撤销、进入更正或姓名发生变化，页面立即把公共房间切回房间昵称，并显示一次性说明：**你的确认姓名发生变化，为保护隐私，已改回房间昵称。确认完成后，你可以重新选择是否公开姓名。** 重新确认后不自动恢复实名。

### 大家的解忧室

- 身份模式：匿名、房间昵称或经验证姓名；选择经验证姓名时二次确认将公开的准确姓名，并说明学校不会因此获得房间访问权或得知本人是否使用实名。
- 房间昵称模式下显示系统昵称和有限的“换一个昵称”操作。
- 交流期待：可选择、更新或清除。
- 学校开关：**向其他同学展示我的学校**，默认关闭。
- 开启时使用二次确认 dialog，明确显示即将公开的验证学校名称。
- 关闭立即生效，显示：**学校已隐藏。平台内你在这个房间的既往消息也不再显示学校标签。**
- 同时提示：**已经看过或截图的信息无法由平台收回。**
- 没有有效验证学校时不显示可操作开关，只显示：**当前没有可展示的验证学校。** 不回退到试点批次学校。

## 11. 交流期待

交流期待是可选、可随时更新的房间状态，不强迫学生每次发消息前选择。

- 只想说出来
- 希望有人听见
- 想听相似经历
- 可以给我一些建议
- 想了解支持资源

选择后，以小标签出现在本人昵称旁；点击可以修改。对于“只想说出来”，其他成员仍可发送轻量“我听见了”，但界面提醒不要追问或给建议。群聊不声称能够技术上禁止别人对某句话回应，因此边界由规则、值班和举报共同执行。

“想了解支持资源”同时提供独立按钮，但不自动把聊天内容带到支持入口。

## 12. 输入区与发送状态

### 常态

- 单行起步、随内容增高的 textarea，最大约 5–6 行后内部滚动。
- 占位：**写下你现在想说的……请不要留下姓名、班级或联系方式。**
- 字数接近 600 时显示计数；达到上限时清楚说明，不能悄悄截断。
- 不显示附件、语音、图片、位置或 @ 提及按钮。
- 发送按钮有明确文字或可访问名称，不能只有图标。

### 状态反馈

| 状态 | 本人界面 | 行为 |
|---|---|---|
| `draft` | 输入框保留文字 | 只保存在本机当前页面，不上传 |
| `sending` | 临时消息呈淡色并显示“发送中” | 禁止同一幂等请求重复点击 |
| `sent` | 恢复正常样式 | 服务端已保存且允许公开 |
| `failed` | 显示“发送失败，重试” | 保留原文和同一幂等键，避免重复消息 |
| `revision_required` | 返回输入框并标出需要修改的边界 | 不向其他学生公开 |
| `safety_review` | 停止普通发送并显示固定现实支持提示 | 不广播，不承诺已有人联系 |

按 Enter 发送只适用于桌面端且提供设置；默认 `Enter` 换行、`Ctrl/Cmd + Enter` 发送，降低误发。移动端点击发送按钮。

## 13. 网络与房间状态

### 连接中

- 历史区域显示轻量 skeleton，输入区暂不可发送。
- 超过合理时间后显示“连接得有点慢”，提供重试和返回成年人入口。

### 正在重新连接

- 顶栏显示“正在重新连接”。
- 保留输入草稿，但不自动排队发送，避免恢复连接后在学生不知情时发出过时内容。
- 重连成功后先补齐历史，再恢复发送按钮。

### 暂时只读

- 顶栏和输入区上方都显示：**现在没有完整值班支持，房间暂时只读。下次计划开放：……**
- 输入区禁用，服务端同样拒绝发送。
- 仍可查看获准历史、本人举报、屏蔽名单和支持资源。

### 已暂停

- 普通聊天历史不继续展示，页面显示暂停原因的用户安全摘要。
- 保留查看本人消息处理状态、本人举报、退出和现实支持入口。
- 不展示内部安全事件、被举报人或具体工作人员信息。

### 资格撤销或房间关闭

- 使用通用不可用页，不继续显示缓存历史。
- 清空内存中的消息和学校展示缓存，取消 Realtime 订阅。
- 操作返回成年人入口，不透露撤销由学校、平台还是哪位工作人员触发。

## 14. 消息操作

### 本人消息

- 操作：删除、复制文字。
- 本人可以随时删除，不另设短时撤回窗口。删除确认说明“平台内将立即停止显示；若有未结举报或安全事件，可能按规则保留必要副本”。
- 首批不提供编辑，避免他人回应后内容含义改变；需要修改时删除后重新发送。

### 他人消息

- 操作：举报这条消息、屏蔽这个房间身份；屏蔽关系绑定内部成员 ID，不因对方在匿名、昵称或经验证姓名之间切换而失效。
- 举报 dialog 沿用现有分类思路，但不在真实排班未建立时承诺 2/24/72 小时。
- 举报提交后目标立即对举报人隐藏，并提供“查看我的举报”。
- 屏蔽确认说明双方内容会互相隐藏，不告诉对方是谁操作；屏蔽不取消平台安全审核。

### 官方消息

- 普通学生不能屏蔽房间规则和安全状态消息。
- 可举报具体值班人员的不当发言，进入独立工作人员复核，不由被举报人本人结案。

### 退出解忧室

- 退出前说明：退出会立即停止读取和接收新消息，不删除本人已经发送的消息；如需删除，应先逐条删除或通过账号注销处理。
- 再次加入后可以继续查看本人此前有效成员期内的内容，并从新的成员期继续接收；离开期间产生的消息始终不可见。
- 主动退出某一房间后，学校验证或规则状态同步不能把本人静默加回；必须由本人明确点击重新加入。
- 退出不影响 YouthTempo 的 SWEET、支持工具和其他本人功能。

## 15. 通知与隐私

- 首批不发送包含聊天正文、对外身份或学校的邮件、短信、浏览器 push 或系统通知。
- 解忧室入口可显示公共房间有未读更新，但不显示“某学校的某位成员对你说了什么”。
- 浏览器标题最多显示“解忧室有新消息”，不包含内容或房间学校。
- 页面退出后清除当前消息内存；持久缓存范围和离线存储必须单独审批。
- 分析事件只记录入口展示、规则确认、进入房间、发送成功/失败、断线、举报等必要动作，不记录正文、对外身份、学校标签组合或安全提示内容。

## 16. 无障碍与双语

- 新增所有用户文案同时加入 `zh-CN` 和 `en`，中文为语义源，键结构完全一致。
- 新消息使用 `aria-live="polite"` 的简短“有新消息”通知，不让屏幕阅读器自动朗读整段敏感正文。
- 房间状态、发送状态和学校开关不能只靠颜色表达。
- 消息更多菜单、dialog、bottom sheet 支持键盘、Escape、焦点锁定和关闭后焦点恢复。
- 触控目标至少 44px；移动端输入字号保持 16px，避免 iOS 自动缩放。
- 尊重 reduced motion；断线重连、消息追加和新消息按钮不依赖快速动画。
- 英文文案保持温暖、简短、不医疗化；房间宽度和按钮不因长英文溢出。

## 17. 页面实现验收标准

- 未受邀成年人和 `14_17` 看不到入口，直接 URL 也不泄露资格或房间信息。
- 受邀成年学生完成一次规则确认后进入公共聊天室；规则更新会重新确认。
- 首次规则确认自动加入公共房间；学校验证不会创建或加入本校聊天室。主动退出后不会被静默重新加入。
- 后来加入者不能通过分页、搜索、旧链接或直接 message ID 读取加入前内容；重新加入者不能读取离开期间的消息。
- 公共聊天室的跨学校范围文案始终可见，不靠学生记忆。
- 学校默认隐藏；只能展示服务端当前验证学校，关闭后当前和既往消息视图均移除标签。
- 公共房间支持匿名、房间昵称、经验证姓名；模式切换同步影响当前和既往消息展示，不允许逐条消息切换。
- 经验证姓名只能来自 active 学校名单确认记录；没有可信来源时不得回退到可编辑 `display_name`、邮箱、自由输入或现有邀请姓名，也不得要求上传身份证件。
- 姓名确认失效或变化会使公共房间安全回退到房间昵称；重新确认后不会自动恢复，须本人重新选择。
- 正常、发送中、失败、复核、断线、只读、暂停和失权状态均有明确且不误导的恢复路径。
- 高风险消息在公开和 Realtime 事件前停止，学生端不声称专业人员已经回应。
- 屏蔽、举报、删除和退出在移动端单手操作可完成，并有服务端允许/拒绝测试。
- 不出现成员列表、精确在线状态、已读名单、私信、联系方式交换或热度排行。
- 桌面、320px 窄屏、真实 iPhone、Android 和微信内置浏览器均完成聊天、软键盘、重连和 dialog 验收。

## 18. 本草案不包含

- 值班人员工作台、排班后台和审核队列的具体页面布局。
- 专业咨询师承接和独立专业支持入口的 UI。
- 数据库、API、Realtime 或 RLS 实现。
- 插图生成、最终视觉稿或页面代码。
- 14–17 岁解忧室界面。

学生端 UX 批准：产品负责人 ______ / ______；隐私或安全负责人 ______ / ______；审核运营负责人 ______ / ______；大学试点方 ______ / ______。
