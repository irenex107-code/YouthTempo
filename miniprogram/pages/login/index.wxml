<view class="page">
  <text class="eyebrow">邮箱验证码登录</text>
  <view class="title">把记录留在自己的账号里</view>
  <view class="subtitle">验证码会发到你的邮箱。YouthTempo 不会向你索要密码。</view>
  <view class="card">
    <input class="field" type="text" placeholder="邮箱" value="{{email}}" bindinput="setEmail" />
    <button class="secondary" bindtap="sendCode" disabled="{{sending || countdown > 0}}">{{countdown > 0 ? countdown + ' 秒后可重发' : sending ? '正在发送…' : '发送验证码'}}</button>
    <input wx:if="{{codeSent}}" class="field" type="number" maxlength="8" placeholder="8 位验证码" value="{{token}}" bindinput="setToken" />
    <button wx:if="{{codeSent}}" class="primary" bindtap="verifyCode" disabled="{{verifying || token.length !== 8}}">{{verifying ? '正在登录…' : '登录'}}</button>
    <view wx:if="{{message}}" class="notice {{isError ? 'error' : ''}}">{{message}}</view>
  </view>
  <view class="muted" style="margin-top: 28rpx;">当前自主试用面向 14 岁及以上学生。14–17 岁完成本人单独确认后即可使用，不需要学校或监护人加入。</view>
</view>
